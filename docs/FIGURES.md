# Model figures — which code generates each

All scripts live in `simulations/` and run against the calibrated **v44 single-step default**
(`src/build_model_v44_heldt.py`). Each writes a `.png` (preview) and a `.pdf` (vector, for the paper)
into `simulations/`. Regenerate any single figure with, e.g.:

```bash
./venv/bin/python simulations/fig_v44_fig5_rescue.py
```

**After any model change, regenerate the whole set with the manifest** (auto-globs every
`fig_v44_*.py` and explicitly runs the slow ensemble figures that don't match that glob, e.g. the
cyclin D1 / birth-p27 bifurcation):

```bash
bash simulations/regenerate_figures.sh
```

The unified validation harness `simulations/validate_v44.py` defines the conditions, the division
counter, the phase (p27) classifier, and the experimental-target checks that the figure scripts import.

---

## Paper figures (model)

| Manuscript panel(s) | Output PDF | Generating script | Notes |
|---|---|---|---|
| **Fig. 5A–C** — Ptch+/− MB / +HHi (arrest) / +HHi+Ezh2i (rescue), Cyclin B traces | `fig_v44_fig5_rescue.pdf` | `fig_v44_fig5_rescue.py` | |
| **Supp. Fig. 7A** — model architecture / wiring diagram | `fig_v44_wiring.pdf` | `fig_v44_wiring.py` | pure schematic (no simulation) |
| **push/pull summary schematic** — what drives EZH2 & CyclinD1 up/down + the H3K27me3 feedback | `fig_v44_pushpull_schematic.svg` | hand-authored SVG (no simulation) | Balance-sheet capstone for the EZH2↔CyclinD1 thread: green = drives up, red = pushes down; EZH2→H3K27me3⊣CyclinD1 leaky-rheostat feedback; three emergent-behavior callouts (progressive repression/buffering; proliferation-rate sets the brake via dilution ∝1/period; **G0 reversible/monostable, not a bistable lock**). Vector; converts to PDF for the manuscript (rsvg-convert/Inkscape). |
| **Supp. Fig. 7B–M** — validation grid (Cyclin B traces; CyclinD1/Mycn/Gli1 model‑vs‑data; EZH2; division counts; time courses) | `fig_v44_validation.pdf` | `fig_v44_validation.py` | |
| **Supp. Fig. 8A–H** — EZH2→CyclinD1 feedback (±feedback: Cyclin B, CyclinD1 mRNA, EZH2 protein, phase relationship) | `fig_v44_feedback.pdf` | `fig_v44_feedback.py` | feedback OFF = `f0_mk = 1.0` (AUM mark cannot repress) |
| **Supp. Fig. 8I,J** — MB+CDK4/6i and MB+CDK4/6i+Ezh2i (no rescue) | `fig_v44_cdk46i_norescue.pdf` | `fig_v44_fig5_rescue.py` | emitted alongside Fig. 5 |
| **Supp. Fig. 7x** — Gli→Ptch1 negative feedback & its disruption in MB (Gli1 step overshoot GNP vs MB; Ptch1 mRNA induction; Smo/Gli broken‑loop) | `fig_v44_ptch1_feedback.pdf` | `fig_v44_ptch1_feedback.py` | Ptch1 = Gli target; `Ptch1_copy_number` = functional fraction (1.0 GNP / 0.1 MB) |
| **Fig. 5C** — population cycling fraction (vismo↓, Ezh2i restores; CDK4/6i no rescue) | `fig_v44_fig5_population.pdf` | `fig_v44_fig5_population.py` | N=120 ensemble, CyclinD1/p27 heterogeneity |
| **Conditions → input parameters + parameter space** (A: input-knob matrix per condition; B: CyclinD1×p16 plane with cycle/arrest boundary + each condition placed) | `fig_v44_condition_paramspace.pdf` | `fig_v44_condition_paramspace.py` | shows MB+HHi arresting at the *same* CyclinD1 a GNP cycles at, because p16 raises the threshold; EZH2i crosses it back |

Drop‑in replacement Results/Methods/captions for the manuscript: `simulations/PAPER_MODEL_SECTIONS_v44.md`.
Data sources used to parameterize the model: `docs/PARAMETERIZATION.md`.

---

## Exploratory analyses (developed while interrogating the model; not current manuscript figures)

| Output PDF | Generating script | What it shows |
|---|---|---|
| `fig_v44_ezh2i_doseresponse.pdf` | `fig_v44_ezh2i_doseresponse.py` | Graded EZH2i dose‑response of CyclinD1 (transcript+protein) and proliferation in GNP / MB / MB+HHi / MB+CDK4/6i — the rescue **threshold** and the no‑rescue control. |
| `fig_v44_ezh2i_population_dose.pdf` | `sim_ezh2i_population_dose.py` | Population EZH2i dose‑response: % of a heterogeneous (cyclin D1/p27) tumour pushed back into cycle vs dose — the "low‑dose rescues a fraction" curve. (~15 min) |
| `fig_v44_feedback_role.pdf` | `fig_v44_feedback_role.py` | WITH vs WITHOUT the EZH2 ⊣ CyclinD1 arm across mitogen: the feedback installs a mitogen‑gated quiescence threshold (cell‑cycle exit). |
| `fig_v44_ckoo_feedback_strength.pdf` | `fig_v44_ckoo_feedback_strength.py` | Is the ~3× CyclinD1 supported by the cKO? Recombination correction of the bulk fold + model feedback‑strength sweep. |
| `fig_v44_g0_mechanism.pdf` | `fig_v44_g0_mechanism.py` | CyclinD1 sets transient‑G0 duration (p27 marker); EZH2i collapses it; arrest below threshold. |
| `fig_v44_cycle_anatomy.pdf` | `fig_v44_cycle_anatomy.py` | One‑cycle anatomy: the transient G0 is the p27‑high window (phospho‑Rb saturates early — why we classify G0 by p27). |
| `fig_v44_g0_bifurcation.pdf` | `sim_g0_bifurcation.py` | Proliferation–quiescence bifurcation from cyclin D1 / **birth-p27** (`P21_div`) heterogeneity (N=140). Low birth p27 → immediate re-entry, high → prolonged transient G0 (the ultrasensitive Fan–Meyer threshold; mapped by `probe_birthp27_dwell.py`); low CyclinD1 → arrest. SHH and EZH2i tune the split (immediate ↑, arrest ↓, dwell ↓). On the baked model. (~10 min) |
| `fig_v44_mitogen_withdrawal.pdf` | `sim_mitogen_withdrawal.py` | **Mitogen-withdrawal experiment on the heterogeneous GNP ensemble (N=16, birth-p27 + CyclinD1-setpoint draws).** Cells equilibrated cycling at high SHH, then mitogen withdrawn SUDDENLY or GRADUALLY to a range of final levels; counts post-withdrawal divisions ± the EZH2⊣CyclinD1 feedback (no-feedback = `f0_mk`→1.0, the H3K27me3 mark cannot repress). (A/B) divisions vs final mitogen, sudden/gradual — the **feedback gap is depth-dependent, maximal at partial withdrawal (SHH 0.15–0.3)** and closes at full withdrawal (both arrest) and at baseline (both cycle); sudden > gradual. (C) per-cell distributions at SHH→0.3: sudden +fb 2.4 vs −fb 4.6; gradual +fb 3.3 vs −fb 4.5. (D) representative cell: no-feedback CyclinD1 stays high → coasts (5 div), feedback represses CyclinD1 → arrests (1 div). State-reuse + tol-retry; `--probe` / `--fresh`. (~2.5 h) |
| `fig_v44_ezh2i_rescue_kinetics.pdf` | `fig_v44_ezh2i_rescue_kinetics.py` | **EZH2i fast-tracks CyclinD1 de-repression → rescues vismo-arrested MB** (uses the calibrated `with_h3k27_memory` model — an explicit H3K27me3 mark at the CyclinD1 locus). Vismo lowers Gli-driven CyclinD1 but the MYCN floor keeps it near threshold; residual H3K27me3 holds it just below, and EZH2i clears the mark to re-tip it. EZH2i blocks deposition → mark cleared by demethylation + replication-dilution → CyclinD1 de-represses over ~24–48h → cycle re-entry. (A) CyclinD1 ± EZH2i; (B) the H3K27me3 mark (mechanism); (C) timed dosing @0/24/48/72h — de-represses whenever given (the fast-track); (D) rescue outcome (5–6 div vs vismo-only's 1) on-demand. **NB (2026-07): the earlier "EZH2-maintained lock-in" framing is superseded** — EZH2 is cell-cycle-gated (falls in G0) and the mark does NOT make arrest bistable (G0 is monostable/reversible, "sticky"/metastable — see `fig_v44_g0_bistability_confirm`); the rescue itself (EZH2i re-tips the MYCN-floored, mark-repressed borderline) is unchanged. `--fresh`. (~15 min) |
| `fig_v44_vismo_withdrawal.pdf` | `sim_vismo_withdrawal.py` | **Vismodegib treatment of the heterogeneous MB ensemble — the MB analog of mitogen withdrawal.** Vismo (GDC0449) blocks Smo, removing Gli-driven CyclinD1 but leaving the MYCN floor → even full vismo is a *partial* withdrawal, so the EZH2 feedback is decisive (= the model's vismo-arrest / EZH2i-rescue). MB cells equilibrated cycling, then vismo applied SUDDENLY/GRADUALLY to a range of doses; counts post-treatment divisions ± feedback (no-fb = `f0_mk`→1.0 ≡ EZH2i/tazemetostat). (A/B) divisions vs dose — feedback gap opens at high vismo. (C) at near-full vismo (GDC=0.95): +fb → uniform arrest (1 div); −fb (EZH2i) → **bimodal rescue** (high-CyclinD1-setpoint cells coast 4–5, low ones arrest = "rescues a fraction"). (D) representative cell: no-fb MYCN-floor CyclinD1 sustains cycling, fb represses → arrest. Capped at GDC=0.95 (GDC=1.0 = Smo singularity, spurious arrest; vismo never 100%-blocks Smo). State-reuse + tol-retry; `--probe`/`--fresh`. (~2.5 h) |
| `fig_v44_ezh2_phaseplane.pdf` | `sim_ezh2_phaseplane.py` | **Ferrell-style phase-plane / bifurcation analysis of the EZH2→H3K27me3 ⊣ CyclinD1 feedback (AUM model).** In the AUM model the direct repressor is the H3K27me3 mark Mk, so the plane is **(Mk, CyclinD1)**. (A) Mk–CyclinD1 nullcline portrait (GNP): Cd-nullcline analytic (Cd fast, mark represses it via `R(Mk)=f0+(1−f0)/(1+(Mk/K)^n)` ↓), Mk-nullcline traced by breaking repression (`f0_mk=1`, sweep drive) so Mk is a readout of Cd (↑); they meet at one stable node (monostable, buffering); fast/slow direction field + limit-cycle orbit overlaid. (B) GNP/MB/+EZH2i — mitogen drive & feedback removal move the operating point. (C) bifurcation vs CyclinD1 drive: feedback shifts the proliferation threshold higher (mean-E2F order parameter). (D) bifurcation vs mitogen (Gli1): feedback raises the mitogen threshold for proliferation (gated quiescence). Two-stage npz cache; `--fresh`. (~35 min) |
| `fig_v44_replicative_dilution.pdf` | `fig_v44_replicative_dilution.py` | **Replicative dilution of H3K27me3 limits EZH2 repression — mitogen sweep, dilution ON vs OFF (AUM model).** Compares the default (S-phase halving `Mk=0.5*Mk`) against the halving disabled (`Mk=1.0*Mk`) across a GNP SHH sweep. (A) Mk(t) at SHH=0.8: ON = sawtooth (halved each division, re-methylates), OFF = smooth & higher — the dilution events visualized. (B) steady Mk vs mitogen: dilution holds Mk below the no-dilution ceiling and **the gap widens with mitogen** (faster cycle → more-frequent dilution; gap 0.075→0.16). (C) steady CyclinD1 vs mitogen: the mark relief → higher CyclinD1 (EZH2 reach limited by proliferation rate). (D) divisions/168h vs mitogen = the dilution frequency (the driver). No cache (~3–5 min). |
| `fig_v44_h3k27_response.pdf` | `fig_v44_h3k27_response.py` | **H3K27me3 → CyclinD1 response characteristics — static transfer functions + parameter sensitivity (algebraic steady state, fast).** (A) leaky‑Hill readout `R(Mk)=f0+(1−f0)/(1+(Mk/K)^n)` — the mark throttles toward a floor `f0=0.233`, never silences; steepness set by `n_mk`. (B) methylation dose‑response `Mk_ss(EZH2)` — read‑write autocatalysis (`k_w_mk`) sharpens it, and **faster cycling dilutes it down** (arrested vs 34 h vs 18 h period). (C) the throttle `R(Mk_ss(EZH2))` — transcript falls away from the no‑brake drive as EZH2 rises (the buffering gap). (D–I) per‑parameter small‑multiples (0.5×/1×/2×): `del_mk`, `K_mk`, `k_w_mk` dominate the response; `f0_mk` sets the high‑EZH2 tail floor; `g_mk` (transcription eviction) is nearly inert at the calibrated weak regime. Transcription‑eviction held at a representative `Cd_mRNA`. |
| `fig_v44_h3k27_timing.pdf` | `fig_v44_h3k27_response.py` | **H3K27me3 → CyclinD1 timing & cell‑cycle impact (full v44 model).** (A) de‑repression kinetics after EZH2i — mark half‑life **t½ ≈ 15 h**, tunable ~2× by `del_mk`; smooth demethylation + step‑drops at each division. (B) replicative‑dilution **sawtooth** — Mk halved every S phase then rebuilds, clocked by the cycle (CyclinD1 overlaid). (C) **mean Mk vs cell‑cycle period** (growth‑rate sweep) — period 12→38 h ⇒ mean Mk 0.25→0.46 and mean transcript inversely (~7→3.5): faster proliferation out‑dilutes the writer, a positive feedback on cycling speed. (D) entry timing on a rising‑Hh ramp — the brake delays first division from **29 h / SHH 0.23** (EZH2i) to **50 h / SHH 0.35** (default). Emitted by the same script as `_response`; ~1–2 min. |
| `fig_v44_cellcycle_duration.pdf` | `fig_v44_cellcycle_duration.py` | **Cell‑cycle duration / growth phase sets the H3K27me3 brake (replicative‑dilution frequency = 1/period).** (A) mark sawtooth at a short (13 h) vs long (35 h) cycle — the longer cycle rebuilds more mark between S‑phase halvings (⟨Mk⟩ 0.27→0.46). (B) dilution ON vs OFF vs period: the "dilution relief" gap shrinks from 0.26 (~10 h) to 0.05 (~54 h) — the impact of replicative dilution diminishes as duration increases. (C) **developmental slowing** — growth rate ramped down → cycle lengthens → mark accumulates → CyclinD1 progressively repressed toward exit (a feed‑forward, NOT a self‑slowing loop: CyclinD1 doesn't set the running period in v44). (D) mean CyclinD1 transcript & division rate vs period. ~2 min. |
| `fig_v44_g0_mark_dynamics.pdf` | `fig_v44_g0_mark_dynamics.py` | **CONSOLIDATED — H3K27me3 dynamics in arrested G0: plateaus, a race, reversible.** (A) arrest time‑course: the mark does NOT run away — it overshoots (dilution stops) then settles to a modest plateau (~0.31, *below* the cycling mean) because the writer EZH2 is E2f‑gated and collapses in G0. (B) phase diagram (reads the `g0_mark_regime` cache): whether the arrested mark ends up above/below cycling is a RACE between EZH2‑writer persistence (half‑life) and mark turnover; the measured EZH2 t½≈77 h places GNPs on the RELAX (reversible) side, near the fence. (C) two‑initial‑condition test (reads the `g0_bistability_confirm` cache): from a high‑mark or low‑mark start the cell settles to the SAME state at every drive → **MONOSTABLE, reversible** (a sharp but single‑valued transition; "sticky"/metastable, NOT a bistable lock). Pulls the two caches + regenerates panel A (~1 min). |
| `fig_v44_g0_mark_regime.pdf` | `fig_v44_g0_mark_regime.py` | **Phase diagram: does H3K27me3 accumulate or relax in persistent G0 arrest?** Sweeps EZH2 protein half‑life (`kDeEZ`; measured G2/G0 = 1.48× IF → ~77 h, plausible range 46–580 h) × mark turnover half‑life (`del_mk`); at each, simulates a cycling (SHH=1.0) and a long‑arrested (SHH=0.1, no dilution) cell and colors (arrested − cycling) mean mark. **RELAX** (writer collapse wins → reversible) vs **ACCUMULATE** (dilution‑loss wins → mark ratchets), split by the exact‑tie contour. Operating point sits in RELAX near the boundary; the measured EZH2‑half‑life box STRADDLES the boundary → the direction is decided by the (G0‑specific) EZH2 stability — the measurement to make. 16×16 grid, cached (~15 min). |
| `fig_v44_g0_bistability_confirm.pdf` | `fig_v44_g0_bistability_confirm.py` | **Definitive bistability test — fixed‑SHH, two‑initial‑condition equilibration.** At each SHH the cell is dropped in from BOTH a high‑mark (arrested) and a low‑mark (cycling) start and integrated to TRUE steady state (stiff‑zone tolerance escalation). In BOTH the calibrated (RELAX) and ACCUMULATE regimes the two starts CONVERGE → **MONOSTABLE**; the mark does NOT create a bistable G0 lock. The apparent hysteresis of the quasi‑static sweep (`fig_v44_g0_hysteresis`, superseded) was a slow‑transient artifact. Conclusion: G0 is reversible/monostable; stable EZH2 + slow mark make arrest **metastable** ("sticky", resolves over hundreds of h), not latched. Cached (~15 min). |
| `fig_v44_g0_hysteresis.pdf` | `fig_v44_g0_hysteresis.py` | **SUPERSEDED** by `fig_v44_g0_bistability_confirm` — a quasi‑static SHH sweep that FALSE‑POSITIVED bistability in the ACCUMULATE regime (the ~230 h‑half‑life mark can't equilibrate in a ~200 h/step sweep, so the arrested branch *looks* locked but is a long transient). Kept as a methodological cautionary record; do not cite as bistability evidence. |
| `fig_v44_paramsweep_{entry,withdrawal,cumprolif,cuts}.pdf` | `sweep_param_exploration.py` → `plot_param_exploration.py` | **HIGH-RESOLUTION parameter sweep** (50×50×10 = 2,500 entry thresholds + 25,000 withdrawal ramps; cached in `sweep_param_exploration_cache.npz`; ~2 h, NOT in the auto-regen). `_entry`: fine mitogen-threshold-for-first-division heatmap over (EZH2 responsiveness `kEZE2f` × inhibition depth `1−f0`) with contours + the no-proliferation (NaN) region. `_withdrawal`: 2×10 small-multiples — exit-Hh (smooth) and withdrawal-division (discrete) heatmaps, one column per Hh decline rate (50→500 h). `_cumprolif`: **cumulative proliferation = fold expansion 2^divisions** (symmetric GNP divisions; median-smoothed, log color), per decline rate — spans 1× (fast decline / strong EZH2) to ~10³–10⁴× (slow decline / weak-unresponsive EZH2). `_cuts`: 1-D line cuts through the default (entry threshold + cumulative proliferation). Re-plot anytime from the cache via `plot_param_exploration.py`. |
| `fig_v44_param_exploration.pdf` | `fig_v44_param_exploration.py` | **Parameter exploration (coarse, superseded by the sweep above): EZH2 mitogen-responsiveness (`kEZE2f`) × CyclinD1 inhibition depth (`1−f0_mk`) × Hh decline rate → entry threshold & withdrawal divisions.** (A) mitogen threshold for first division over (responsiveness × inhibition) — both knobs raise the threshold monotonically, spanning SHH 0.11 (weak) → 1.99 (strong), default ~0.34. (B) divisions during a 150 h Hh withdrawal over the same plane — stronger/more-responsive EZH2 → fewer divisions (0 at strong EZH2). (C) withdrawal divisions vs Hh decline rate for 4 settings — slower decline → more divisions; strong inhibition caps at 0, no-EZH2 gives the most. Entry threshold by SHH bisection; withdrawal divisions are discrete counts (trends robust, exact values noisy). Cached (`--fresh`); ~5 min. |
| `fig_v44_progressive_repression.pdf` | `fig_v44_progressive_repression.py` | **The core consequence: EZH2 is mitogen-responsive → progressive H3K27me3 repression of CyclinD1.** GNP Hh (SHH) dose sweep, steady state. (A) the brake tracks the gas — Gli1 (Hh activity) AND EZH2 + H3K27me3 both rise with Hh. (B) CyclinD1 raw drive (Gli+MYCN, un-repressed = Cd_mRNA/R) rises 5.7× while the actual transcript rises only 2.5×; the shaded gap = amount repressed by H3K27me3, growing with Hh. (C) surviving fraction R = transcript/drive falls **80% → 35%** across the Hh range — the repression is *progressive*. (D) **Hh needed to INITIATE proliferation** — division rate vs Hh with vs without EZH2 repression: EZH2 raises the proliferation threshold from ≤0.08 (basal drive suffices) to SHH ≈ 0.35 — a mitogen gatekeeper. Steady-state (clean, no cell-cycle noise). |
| `fig_v44_cellcycle_exit.pdf` | `fig_v44_cellcycle_exit.py` | **Linear Hh WITHDRAWAL — when the cell cycle ends, with vs without EZH2 repression.** SHH ramped 1.0→0.05 over ~150 h from a cycling start; default vs EZH2i. (A) cycle-avg CyclinD1 vs time with the commitment threshold; EZH2 keeps CyclinD1 lower so it exits **earlier, at higher Hh (SHH≈0.54)** while EZH2-inhibited cells cycle down to **SHH≈0.18** — mirror-symmetric with the entry thresholds (~0.55 vs ~0.20). Exit scored as cycle-avg CyclinD1 dropping below a nominal commitment threshold (2.0 a.u.); "last division" is a noisier proxy. (B) epigenetic state: as Hh withdraws EZH2 **falls** (cycle exit → E2F down), so the GNP arrest is **reversible** — the mark is not sustained the way MB (MYCN-sustained EZH2) locks it in (the EZH2i-rescue contrast). Cached (`--fresh`); ~3 min. |
| `fig_v44_cellcycle_entry.pdf` | `fig_v44_cellcycle_entry.py` | **When a GNP enters the cell cycle under gradually rising Hh — with vs without EZH2 repression of CyclinD1.** SHH ramped 0→1.0 over ~150 h from an arrested start; default (EZH2 active) vs EZH2 inhibited (EZH2i=1). (A) CyclinD1 (cycle-avg) + rising SHH; cell-cycle-entry marker (first division) for each. (B) cumulative divisions — the lift-off = entry. EZH2-inhibited cells commit early (**t≈50 h, SHH≈0.22**); with EZH2 active, commitment is delayed to a higher Hh (**t≈106 h, SHH≈0.59**) — EZH2 gates cell-cycle entry to a genuine Hh threshold (~2.7× more Hh, ~56 h later). Cached (`--fresh`); ~3 min. |
| `fig_v44_progressive_repression_timecourse.pdf` | `fig_v44_progressive_repression_timecourse.py` | **Time-course of progressive repression: a GNP under GRADUALLY rising Hh.** SHH ramped 0.2→2.0 over ~180 h; cycle-averaged. (A) Gli1 (gas) tracks Hh quickly while the slow, mitogen-responsive EZH2→H3K27me3 brake rises behind it (the lag). (B) the CyclinD1 drive climbs 5.7× but the actual transcript only ~4×; the surviving fraction R transiently *rises* early (gas outpaces the slow brake) then *progressively falls* (0.8→0.36) as H3K27me3 accumulates — the repression developing in real time. Cached (`--fresh`); ~3 min. Companion to the steady-state `fig_v44_progressive_repression`. |
| `fig_v44_dilution_tension.pdf` | `sim_ezh2_dilution_tension.py` | **The H3K27me3 tension: EZH2-driven methylation (integrates S-phase + mitogen) vs replicative dilution → CyclinD1 transcript.** (A) Cd_mRNA over (mitogen SHH × S-phase duration) — the physiological tension surface (long-S+low-mitogen → mark wins → Cd low; short-S+high-mitogen → dilution wins → Cd high). (B) S-phase-duration sweep: EZH2↑, mark↑, Cd_mRNA↓ (EZH2 integrates S-phase). (C) control-parameter sweeps (×default): deposition knobs `k_w_mk`/`kEZE2f` push Cd_mRNA **down**, removal knobs `del_mk`/`kDeEZ` push it **up**. (D) k_w_mk sweep, dilution ON vs OFF (gap = dilution relief). Cached; `--fresh`. (~10 min) |
| `fig_v44_dilution_tension_variants.pdf` | `sim_ezh2_dilution_tension_variants.py` | **HU-axis + MB/vismo variants of the tension surface.** (A) HU-axis GNP surface: Cd_mRNA over (mitogen SHH × **HU** dose) — HU (replication stress) elongates S → more EZH2 → more mark → less CyclinD1, the experimentally-actionable lever. (B) MB/vismo surface: Cd_mRNA over (vismodegib HHi × HU), MB context — deepest repression at high vismo + high HU. (C) HU decomposition (GNP): S 5→12 h raises EZH2/mark, lowers Cd_mRNA. (D) vismo decomposition (MB): HHi↑ → Gli-drive↓ → Cd_mRNA↓ toward the MYCN floor, while EZH2/mark **fall** (arrest) — vismo lowers CyclinD1 by drive-removal, residual mark sustains arrest. Cached; `--fresh`. (~13 min) |
| `fig_v44_mitogen_buffering.pdf` | `sim_ezh2_mitogen_buffering.py` | **EZH2 buffers CyclinD1 against mitogen variation — protecting GNP transit-amplifying divisions.** (A) sensitivity + onset: steady CyclinD1 & division rate vs sustained SHH ± feedback — feedback flattens the dose-response (gain 0.85→0.47) and sets the proliferation threshold. (B) fluctuation frequency-response: CyclinD1 fractional transfer gain |δCd/Cd|/|δSHH/SHH| vs fluctuation period (cell-cycle isolated by Fourier projection) ± feedback — slow EZH2 **buffers fast mitogen noise** (gain<no-fb, Berry–Howard low-pass filtering) with a resonance near its ~days timescale. (C) response to partial withdrawal (SHH 0.7→0.3) ± feedback — feedback sharpens the exit decision (ON arrests, OFF coasts). (D) commitment memory: sustained withdrawal → exit vs a transient dip → the committed cell completes its division (Rb–E2F R-point memory → no lost progeny). Robust chunked driver (per-chunk tol-retry + saveState); cached; `--fresh`. (~20 min) |
| `fig_v44_mitogen_sensitivity.pdf` | `fig_v44_mitogen_sensitivity.py` | **mitogen sensitivity in Hh-activity (Gli1) units**, with the correct knobs: (A) **GNP — SHH** is the mitogen (sweep up; vismo only extends the axis below the SHH=0 basal floor); (B) **MB — vismodegib** titrates the tonically-high Hh DOWN toward the MYCN floor (MB CKI tones on). Each: CyclinD1 vs Gli1 ± the EZH2⊣CyclinD1 feedback — the feedback represses/flattens CyclinD1 (lower mitogen sensitivity) and raises the cycling threshold (GNP +fb Gli1~0.008, MB +fb ~0.015; arrest region shaded). NB the basal floor still sits close to threshold (small sub-threshold range). Caches; `--fresh`. (~12 min) |
| `fig_v44_mitogen_paramspace.pdf` | `fig_v44_mitogen_paramspace.py` | **2‑D mitogen‑sensitivity planes**: proliferation (divisions/168 h) over (A) SHH × EZH2/H3K27me3‑feedback strength `f0_mk` (leaky floor; lower = stronger) and (B) SHH × p16, with the cycle/arrest boundary drawn and the defaults marked. The Hh threshold to proliferate **rises** with both stronger feedback and more CKI. Cached grids; `--fresh` recomputes. (~20–30 min cold) |

---

## Distribution model (population-of-models; single-cell-data-grounded)

Virtual-cell populations where each cell draws a small set of DECISIVE parameters from log-normal
distributions. **Only the distribution SHAPE and its CV (variance/mean) are used — never the absolute
means** (fluorescence a.u. are arbitrary); draws are median-1 multipliers, so the mean floats on the
model's own scale. CyclinD1-setpoint spread is grounded on measured single-cell CyclinD1 (G0/1 CV 0.70);
p27 and EZH2 axes are placeholders pending the cleaned p27 fit and the mCherry-Ezh2/TMP series.

| Output PDF | Generating script | What it shows |
|---|---|---|
| `sim_distribution_rescue.pdf` | `sim_distribution_rescue.py` | **Fractional EZH2i rescue — THREE measured-variation axes.** Cells draw CyclinD1 abundance (`cd_scale` CV **0.70**), birth p27 (`P21_div` CV **0.33**; G0 bimodality emerges from the switch), and **EZH2 abundance** (`kTlEZ` CV **~0.55**) — all **DATA-grounded** (MB55 single-cell IF), scale-free (median-1 multipliers). Run through the MB engine ± vismodegib ± EZH2i. **Fractional rescue emerges**; rescue depends on CyclinD1 potential AND EZH2 (more mark to lose). Emergent: EZH2 abundance noise propagates into CyclinD1 spread (model CyclinD1 CV 0.47→0.63 once EZH2 varies). (A) CyclinD1 dist. vs measured log-normal; (B) rescue phase diagram in **(CyclinD1 × EZH2 abundance)** — maps onto the coming mCherry-Ezh2 × CyclinD1 single-cell scatter; (C) rescued fraction. `--full` = N=400 population; default pilot N=24. EZH2-σ calibration deferred to the joint mCherry data. |
| `sim_cyclind1_variance_buffering.pdf` | `sim_cyclind1_variance_buffering.py` | **EZH2 buffers cell-to-cell CyclinD1 variance (scale-free, CV only).** Heterogeneous CyclinD1 setpoint; steady CyclinD1 measured WITH vs WITHOUT EZH2 repression (EZH2i). (A) distributions ÷mean compress under repression; (B) per-cell — EZH2 pulls high-setpoint cells down more (nonlinear high-end cap → variance shrinks); (C) CV comparison. **Prediction: EZH2i broadens the single-cell CyclinD1 CV, mainly the high tail.** |
| `sim_cyclind1_variance_calibrate.pdf` | `sim_cyclind1_variance_calibrate.py` | **Calibrated variance-buffering prediction.** Precomputes the setpoint→CyclinD1 transfer function (EZH2 on/off) once, then computes CV_on/CV_off for any input spread. Solves for σ* where the model's *with-EZH2* CV reproduces the measured **0.70** (σ*=0.73 ≈ measured G2 sdlog 0.738), and reads off the predicted *without-EZH2* CV (~**0.82**) and buffering factor (~**1.18×**). Scale-free (only the measured CV). |
| `sim_p27_bimodality.pdf` | `sim_p27_bimodality.py` | **Bimodal G0 p27 emerges from the transient-G0 / immediate-re-entry commitment switch.** UNIMODAL birth-p27 input → the Skp2–p27–E2f switch splits 2N (G0/G1) p27 into two modes (high uncommitted / low committed), **~4.7× apart, matching the measured ~4× G0 two-component separation**. Modest IF floor (observable offset, not a dynamics change). Open test: model high-component CV 0.44 vs data G0-high 1.29 — the excess data breadth predicts the **arrested/poised contamination the cleaned G0 fit should remove**. |

| `sim_population_mitogen_ramp.pdf` | `sim_population_mitogen_ramp.py` | **Population mitogen ramp — H3K27me3 repression gates ENTRY & EXIT.** Heterogeneous GNP population (three measured-variation axes) ramped SHH up (entry) and down (exit), WITH vs WITHOUT the mark repression (`f0_mk`=0.233 vs 1.0). The mark raises the median Hh threshold to **enter** (0.32→0.54) and to **exit** (0.30→0.46) — a symmetric mitogen gatekeeper. (A) entry CDF, (B) still-cycling CDF, (C/D) threshold distributions. NB this slow-ramp *threshold gating* is the steady-state face of the same slow integrator that *buffers fast dips* (`fig_v44_mitogen_buffering`); the two are timescale-complementary, not contradictory. `--full`=N=120. |
| `sim_ramp_parameter_exploration.pdf` | `sim_ramp_parameter_exploration.py` | **What shifts the Hh thresholds for ENTRY & EXIT: H3K27me3 strength × cell-cycle duration × other features.** Robust metrics (bisection entry threshold; scale-invariant exit threshold = SHH where cycle-avg CyclinD1 drops below half its cycling value). (A/B) entry & exit thresholds over (H3K27me3 strength `1−f0_mk` × cell-cycle duration via `mu`) — both rise with stronger mark AND longer cycle (they interact: short cycle *dilutes* the mark, defeating even a strong mark). (C/D) sensitivity tornados. **Key asymmetry:** ENTRY is dominated by intrinsic features (H3K27me3 strength ≫ CyclinD1 drive > CKI > EZH2 level), while cell-cycle **duration** and **S-phase length (HU)** are the *biggest* movers of EXIT but nearly irrelevant to ENTRY — because those act through the mark only when the cell is cycling (dilution/S-phase), which it is during withdrawal but not before entry. NB roadrunner `reset()` resets species but NOT parameters — every sweep call must re-establish a full baseline (else values leak). |
| `sim_ramp_timing_exploration.pdf` | `sim_ramp_timing_exploration.py` | **TIMING sweep (dense grid): how the H3K27me3→CyclinD1 kinetics (mark turnover `del_mk`, EZH2 half-life `kDeEZ`) and replicative-dilution frequency (cycle period via `mu`) set the entry/exit HYSTERESIS as a function of mitogen-ramp SPEED.** The mark is a slow integrator: on a FAST ramp it lags the mitogen and a hysteresis opens between the ramp-up (entry) and ramp-down (exit) Hh thresholds; on a slow/quasi-static ramp the lag vanishes. 20×16 heatmaps of the gap over (ramp duration × timing value). **(A) del_mk & (B) kDeEZ are clean**: hysteresis biggest at FAST ramp + SLOW kinetics (top-left), i.e. the mark acts as a low-pass filter whose cutoff is set by turnover + EZH2 stability. **(C) mu is confounded** (changing cycle period also changes the cell-cycle *response time*), and **(D) the exit threshold is jaggy** — both because the crossing-threshold metric is per-measurement noisy (does NOT improve with grid density → metric-bound, not undersampled). Superseded readout: the CyclinD1-level-vs-SHH **area** hysteresis (`sim_ramp_hysteresis_area`) is the smooth fix. |
| `sim_ramp_hysteresis_area.pdf` | `sim_ramp_hysteresis_area.py` | **Hysteresis as a smooth AREA (supersedes the threshold-gap readout).** Instead of the per-measurement-noisy entry/exit *crossing* thresholds, this measures the **area of the CyclinD1-vs-SHH loop**: at each SHH, cycle-averaged CyclinD1 on the up-ramp minus the down-ramp, integrated over SHH and normalized by the cycling CyclinD1 (dimensionless, scale-free). **(D) is the headline** — the actual loop: the ramp-UP curve (mark lags low → CyclinD1 HIGH) rides well above the ramp-DOWN curve (mark lags high → CyclinD1 LOW), the gold area between them being the metric; smooth, no threshold jitter. Heatmaps (A) `del_mk`, (B) `kDeEZ`, (C) `mu` show the loop area over (ramp duration × timing). **Pre-equilibration is scaled to the kinetic timescale** (4 turnover-times of the slowest of mark/EZH2, capped 333 h) so the down-ramp always starts from a fully-loaded/repressed mark — a *fixed* preeq under-loads slow marks and artificially collapses the slow-kinetics corner (finite-preequilibration confound, same class as the `reset()` leak). Same DEFAULTS-baseline / capped-movavg discipline as `sim_ramp_timing_exploration`. |
| `sim_ramp_hysteresis_trace.pdf` | `sim_ramp_hysteresis_trace.py` | **Worked cell-cycle trace through a single triangle mitogen ramp (SHH up then down, 150 h/leg, default timing)** — the intuition behind the loop. (A) SHH triangle. (B) CyclinD1 sawtooth + H3K27me3 mark: the mark ACCUMULATES during low-SHH arrest (no divisions to dilute it), peaks just before re-entry, then sawtooths down as each division halves it; it lags SHH. (C) EZH2 writer + mass with division ticks (12 divisions up-leg, 14 down-leg). (D) the CyclinD1–SHH loop drawn by this one run: **entry at SHH 0.32, exit at SHH 0.09** — the cell re-enters late (arrest-accumulated mark represses CyclinD1) and keeps cycling to *lower* SHH on withdrawal (mark diluted). **This is the TRANSIENT/kinetic entry-exit hysteresis** (developmentally relevant: cell never dwells at max SHH). Distinct from the EQUILIBRIUM feedback hysteresis in `sim_ramp_hysteresis_area`, whose long (~308 h) high-SHH pre-equilibration lets EZH2 build the mark to its high-SHH steady state and cap CyclinD1 (~2.4 vs ~7.5) — a larger, dwell-dependent effect the triangle doesn't sample. |
| `sim_singlecell_withdrawal_g0.pdf` | `sim_singlecell_withdrawal_g0.py` | **Single-cell ensemble (N=1000, PARALLEL via multiprocessing) through Hh withdrawal — recovers the transient G0 & period-lengthening that cycle-averaging hides.** Each cell draws its own cycle period (`mu`), mark turnover (`del_mk`), EZH2 t½ (`kDeEZ`), birth-p27 (`P21_div`), CyclinD1 setpoint (`k_Cd_translation`); paired across conditions (same cells, only f0_mk differs). Protocol: settle cycling at SHH 1.0 → partial withdrawal to 0.35 over 100 h → hold. Readouts (probe_transient_g0 conventions): divisions=MPF peaks; **G0 = pre-S ∧ p27>0.1**; a ≥2 h G0 run that later resolves into a division = TRANSIENT G0, one that never resolves = ARREST. (A) WITH-mark single-cell raster (green cycling / orange transient-G0 / dark arrest, SHH overlay): a continuous gradient of arrest times with dense **recurring transient-G0 speckle** — the periodic G0 the smooth traces miss. (B) 3 example long-cycling cells: CyclinD1 oscillations **slow & shrink** while p27 rises into G0. (C) inter-division interval **aligned to each cell's own arrest** (arrested cells only; survivorship-free — the naive by-division-number-from-start spuriously DECREASES because only intrinsically-fast cells reach high k): the interval rises monotonically into G0 (~26 h three cycles out → ~65–75 h in the last cycle) — cells **slow ~2.5× approaching arrest**, and this is **the same WITH vs WITHOUT** (if anything the mark-off last cycle is a touch longer). (D) **the mark's real effect: it recruits more cells into arrest, faster** — at 0.35 WITH reaches ~52 % arrested by 100 h vs WITHOUT ~20 %; by 300 h 81 vs 72 %. **Correction from the earlier N=28 run:** the "1st cycle longer WITH the mark" was small-sample noise (at N=1000 k=1 is ~70 h WITH vs ~75 h WITHOUT); the mark does NOT lengthen the per-cell period beyond the generic slow-into-arrest — its withdrawal effect is a higher/earlier ARREST probability. **`--shallow` → SHH=0.50 companion (`sim_singlecell_withdrawal_g0_shallow.pdf`)**, just above the repressed exit threshold (~0.48): same picture, 78 vs 71 % arrested by end. Run: `--n=1000 [--shallow] [--workers=9]`. |
| `sim_mark_fluctuation_response.pdf` | `sim_mark_fluctuation_response.py` | **How do WITH vs WITHOUT the mark respond to FLUCTUATIONS in Hh? Frequency response.** Drive Hh(t)=0.45+0.30·sin(2π t/T) at drive periods T=12–150 h (near the commitment threshold); ensemble (calibrated + mu + partition), phase-binned over the drive. **Answer: essentially the same — the mark is NOT a fluctuation filter.** (A/B) CyclinD1 tracks Hh at all frequencies; WITH mark = lower mean (repressed ~2×) but the **relative** swing is identical. (C) CyclinD1 relative modulation is ~0.52 and **flat & mark-independent** → the mark SCALES CyclinD1, it does not low-pass filter it. The G0/commitment fraction IS low-pass filtered (modulation 0.05 at T=12 h → peak ~0.55 at ~55 h) — proliferation is buffered against fast fluctuations — but the WITH and WITHOUT curves **overlap**, so that buffering is the **cell cycle + Skp2-p27 commitment switch, not the mark**. (D) the mark's own oscillation grows with drive period (it's a slow integrator that can't follow fast drives) but is tiny (0.01–0.08 of Mk), so it doesn't propagate. **Interpretation:** the mark is a slow *mean/trend*-tracker — it integrates sustained changes (→ the entry/exit hysteresis on ramps) but averages out zero-mean fast fluctuations; the repression is set by the mean Hh, and fluctuations pass through mark-independently. *(Same script also emits `sim_mark_fluctuation_transientg0.pdf`.)* |
| `sim_mark_fluctuation_transientg0.pdf` | `sim_mark_fluctuation_response.py` | **Same fluctuation experiment, read out as TRANSIENT G0** (the ≥2 h p27-high pre-S dwell that *resolves* into a later division — state 1; distinct from terminal arrest). Uses a consistent ≥300 h recording so the transient-vs-arrest call is well-determined even for fast drives. **(A/B)** transient-G0 swings ANTI-phase with Hh (high at low Hh); **(C)** its modulation is **bandpass** — peaks at a ~55 h drive (≈2–3 cell cycles), buffered at fast drives — and, unlike the division-rate/commitment readouts, **it tracks the fluctuation MORE with the mark** (peak modulation 0.80 vs 0.67 (N=1000)). This is the mark **SENSITIZING** the quiescence response: by repressing CyclinD1 down to the commitment threshold, small Hh fluctuations tip cells in/out of transient G0 more. **(D)** but the MEAN transient-G0 is LOWER with the mark (0.12–0.22 vs 0.21–0.25) because it's shifted to ARREST (dashed: WITH 0.15–0.23 vs WITHOUT 0.03–0.06) — the same transient-G0→arrest conversion seen in withdrawal/steady-state. So the mark makes transient G0 more *responsive* to fluctuations but *rarer* on average (more of the quiescence is deep arrest). |
| `sim_mark_dip_duration.pdf` | `sim_mark_dip_duration.py` | **Response to a transient Hh DIP of varying DURATION** (not a sine) — the natural probe of the mark's MEMORY. Baseline SHH=0.8 (cycling) → dip to 0.15 for duration D → return → 250 h recovery; ensemble (calibrated + mu + partition), N=500. **The mark makes cells far more vulnerable to a transient dip.** (B) fraction that NEVER recovers (permanent arrest) vs dip duration: WITH mark rises steeply 0.24→0.80 (50% arrest at a **~35 h** dip), WITHOUT stays low 0.02→0.38 (never reaches 50%, even for a 250 h dip). So a ~40 h dip that unmarked cells mostly ride through commits ~75% of *marked* cells to permanent exit. (A) time-domain (48 h dip): WITH-mark arrest commits *during* the dip (~20 h) and doesn't reverse; WITHOUT largely recovers. (C) among cells that recover, recovery is slower WITH the mark at short/mid dips (memory), before survivorship (only robust cells recover) takes over at long dips. (D) the quiescence response *during* the dip is deeper WITH the mark (p27-high G0 occupancy 0.36→0.70 vs 0.29→0.47). **Mechanism = the mark accumulated during the low-Hh dip (no divisions to dilute it) keeps CyclinD1 repressed after Hh returns → a brief, recoverable dip becomes a lasting commitment to quiescence: an epigenetic memory of the Hh loss.** This is the same slow-integrator that filters *fluctuations* (sim_mark_fluctuation_*) acting on a *sustained* change — cf. the ramp hysteresis. |
| `sim_gnp_developmental.pdf` | `sim_gnp_developmental.py` | **The whole GNP developmental proliferative period — WITH vs WITHOUT H3K27me3 at MATCHED output (target MEAN ~8 divisions, tail capped).** Resolves the equal-Hh confound in the earlier dip/ramp figures: because the mark shifts the Hh dose-response, a marked cell needs a HIGHER Hh drive to divide, so comparing at the *same* Hh is unfair. Hh follows a developmental trapezoid (ramp up 48 h → plateau 100 h → ramp down 48 h → short 24 h tail). **Two levers set by a 2-D Hh×window sweep (JP):** the plateau LEVEL sets the mean/entry-floor, the plateau DURATION caps the fast-cycler TAIL (the earlier 140 h window let high-μ/low-p27 cells cycle ~10 h for the whole window → a tail out to ~25 divisions; window→max scales 40 h≈12, 70 h≈16, **100 h≈18**, 130 h≈20). The 100 h window + **WITH plateau 0.75 / WITHOUT 0.22 (~3.4× higher Hh WITH the mark)** holds both at **mean 8.3 vs 8.5** (median 9 both, IQR 7–11, **p95 14, max ~18** — tail halved from ~25). N=300/cond, calibrated abundance + mu + partition, Dna-reset detection; short tail so the count is the Hh-ON developmental period (post-withdrawal coasting excluded). **(A)** the two Hh trajectories. **(B)** distributions overlap at mean ~8 with the capped tail; both carry a zero-division spike. **(C) the payoff — population dynamics:** entry on ramp-up & exit on ramp-down are similar during the window (peak cycling ~0.82 both), but on the ramp-DOWN the **marked cells exit MORE completely** (WITH cycling → ~0.07 vs WITHOUT coasting ~0.27) — the mark enforces a decisive developmental shutdown (and unmarked cells keep coasting/dividing at the withdrawn Hh floor, which is why counting stops at the short tail). **(D) counter-intuitive:** at matched output marked cells sit at *higher* CyclinD1 (~4.4 peak vs ~2.65) because the ~3.4× higher Hh OVER-compensates the repression. **Irreducible mark feature:** ~13% of marked cells never divide at ANY Hh (the repressed low-CyclinD1 tail below the entry threshold); the unmarked condition can be pushed to frac-zero 0 (short window + high Hh). **Interpretation: the mark is not a proliferation brake at fixed output — it re-sets the Hh operating point (higher drive, higher CyclinD1, same mean division count), leaves a residual non-dividing subpopulation, and sharpens the exit at the end of the developmental window.** cf. the entry/exit hysteresis (`sim_mark_lifecycle_hysteresis`); the confounded equal-Hh dip figures (`sim_mark_dip_*`) are superseded by this matched design. |
| `sim_gnp_developmental_ezh2.pdf` | `sim_gnp_developmental.py` (companion) | **EZH2 abundance over the developmental period, WITH vs WITHOUT the mark at matched output.** Emitted alongside `sim_gnp_developmental.pdf` from the same cache. **(A)** mean EZH2 rises & falls with the Hh trapezoid in both arms (EZH2 is a Rb-E2f target → cycle/mitogen-tracking), peaking ~0.95–1.0 at the plateau; the two arms **CONVERGE** despite 3.4× different Hh, with WITHOUT marginally higher through the ramps. **(B)** EZH2 (solid) vs CyclinD1 (dashed) in the same cells — both rise together within a ramp (cycle-driven), the EZH2 ⊣ CyclinD1 feedback. **Key point (verified by a steady-state check, scratchpad):** EZH2 *does* track Hh dose monotonically when unmatched (mean EZH2 0.51→1.15 as constant Hh 0.15→1.0), BUT at the two arms' actual configs the steady levels converge — **WITH (Hh 0.75, mark ON) EZH2 1.02 ≈ WITHOUT (Hh 0.22, mark OFF) EZH2 1.05** — because output is matched and EZH2 is downstream of the cell cycle. The WITHOUT arm reaches the same EZH2 a different way: mark-off de-represses CyclinD1 (Cd 1.67→2.91 at Hh 0.22), which drives cycling (6.6→11.9 div) and pulls EZH2 up. So *matching proliferation matches EZH2* — the apparent "lower-Hh arm has more EZH2" is a matched-output/timing artifact, not a dose inversion. |
| `sim_ezh2_kdeez_sweep.pdf` | `sim_ezh2_kdeez_sweep.py` | **EZH2 protein-degradation (`kDeEZ`) sweep — impact on the Hh-withdrawal ramp, checked against the measured within-cycle phase ratio.** Motivated by IF (protein) + scRNA-seq (RNA) showing EZH2 drops substantially in sustained G0, i.e. EZH2 is *not* very stable — vs the model's baked `kDeEZ`=0.00015 (**t½ ≈ 77 h**). Sweep `kDeEZ` over t½ 77→1.4 h (runtime override; model file untouched); GNP, Hh 0.75, mark ON, N=60. **(A)** EZH2 ÷ pre-withdrawal cycling level vs time: the current 77 h protein only falls to **~0.60 and plateaus** (no divisions to dilute, degradation too slow) — the persistence seen in `sim_gnp_developmental_ezh2`; any t½ ≤ ~23 h drops it to **~0.35**. **(B)** BUT the within-cycle EZH2 phase ratio (measured G0:1/G1:1.16/S:1.31/**G2:1.50**) is *overshot* even at 77 h (model 1.79) and blows up with faster `kDeEZ` (2.0→4.2×). **The naive lesson: a single constitutive `kDeEZ` can't give both a modest within-cycle contrast AND a big G0 drop** — resolved by `sim_ezh2_phase_fit` (the real fix is baseline transcription, not `kDeEZ`). |
| `sim_ezh2_phase_fit.pdf` | `sim_ezh2_phase_fit.py` | **EZH2 protein-by-phase fit: baseline transcription (`kEZbas`) × protein stability (`kDeEZ`) vs the measured phase ratios.** Resolves `sim_ezh2_kdeez_sweep`'s apparent tension. Biological read (JP): G0 has substantial *ongoing* EZH2 transcription (not leftover protein) + an S-phase boost → protein rises in S, peaks G2, drops at division. The model already has this structure (`kEZbas` baseline + `kEZE2f` E2f×cyclin boost) but **`kEZbas`=0.00027 is ~15× too low**, so model G0 is near-pure leftover and the mRNA phase contrast is a wild **5.9×** (data ~1.9–2.2×). 2-D fit (runtime overrides, N=40/combo). **Key result: raising `kEZbas`→~0.004 fixes BOTH** the protein ratio (1.74→**1.48×**, matches 1.50) AND the mRNA ratio (5.9→**~2.0×**, matches scRNA-seq — this also fixes the documented "transcript S/G0 gradient too steep" worst-miss). **(A)** protein-fit SSE landscape — the valley sits at `kEZbas`≈0.004; ○ marks mRNA-also-~2× cells, ★ the joint optimum. **(B)** best-fit protein (1.5×) & mRNA (~2×) by phase vs data. **(C)** single-cell EZH2 across cycles — S-rise/G2-peak/division-drop on an ongoing baseline. **Crucially, raising baseline DECOUPLES the phase ratio from `kDeEZ`** (protein fits for t½ 23–77 h), so the phase data no longer fights the G0-withdrawal-drop data — `kDeEZ` is then free to be pinned by the withdrawal fold (`sim_ezh2_kdeez_sweep` panel A). **Exploratory recalibration** (runtime only): adopting `kEZbas`≈0.004 in the model would need full re-validation (EZH2⊣CyclinD1 repression, MB/GNP=2.05×, dose-response all depend on EZH2 level). |
| `sim_ezh2_dmso_hu_fit.pdf` | `sim_ezh2_dmso_hu_fit.py` | **Finer EZH2 rate fit vs the MB55 24h drug-treatment IF + scRNA-seq — DMSO + HU only (MB context).** Follow-up to `sim_ezh2_phase_fit` with (i) more granularity and (ii) the full 4-treatment experiment. **Critical finding: Palbociclib (`kPhRbCd=0`) and RO3306 (`k25=0`) both COLLAPSE the model cell cycle to a 2N low-E2f state** (classified 100% G0 — confirmed in the `sim_ezh2_phase_validation` cache too, so it's a pre-existing limitation, not a harness bug). RO3306 should arrest at 4N/G2; the model gives 2N. So those two treatments can't constrain EZH2 params until the arrest analogs are fixed (a cell-cycle-model / model-file change). Fit therefore uses DMSO (within-cycle protein profile) + HU (S-arrest boost: S 1.71, G1 1.50 ÷DMSO-G0) + mRNA (scRNA-seq S 1.87 / G2 2.17). Fine `kEZbas`×`kDeEZ` grid (14×11) + 1-D sensitivity of the other rate vars. **(A)** SSE landscape — broad shallow valley (2 params under-determine it). **(B)** best fit still **overshoots DMSO protein** (G2 1.85 vs 1.50) and **under-captures the HU-G1 boost** (1.22 vs 1.50) → the MB-context model over-contrasts EZH2 vs the MB55 data, and 2 params can't fix it. **(C)** secondary-var sensitivity: fit improves with **higher `kEZE2f`** (E2f-gated transcription) and **lower `kDeEZm`** (more stable mRNA); `Kez_cd` and S/G2-gate `wCe` are minor. **Conclusion: a joint `kEZbas×kEZE2f×kDeEZ×kDeEZm` optimization is the needed next step;** Pal/RO await an arrest-analog fix. Related: [[ezh2-baseline-transcription-fix]]. |
| `sim_ezh2_joint_fit.pdf` | `sim_ezh2_joint_fit.py` | **Joint 4-parameter EZH2 rate optimization** (`kEZbas × kEZE2f × kDeEZ × kDeEZm`) vs DMSO+HU protein + scRNA-seq mRNA (MB55, MB context; parallel sample-and-refine, N=22). SSE improved **0.064→0.030** over the 2-param fit. **Matched:** HU-S (1.70 vs 1.71), HU-G0 (1.05), mRNA-S (1.89 vs 1.87), mRNA-G2 (2.03 vs 2.17); HU-G1 improved (1.34). **Concrete param finding:** `kDeEZm`→~0.0016 (**t½ ~7 h, ~12× slower than the default 35 min**) — EZH2 mRNA should be much more stable than baked; `kDeEZ` stays stable (77–144 h, confirmed); `kEZbas` modest ↑ (~2×); `kEZE2f` ~1.3×. **STRUCTURAL MISS (robust): the DMSO within-cycle protein S/G2 overshoots** (model 1.60/1.78 vs data 1.31/1.50) — the model's protein tracks its mRNA (contrast ≈ mRNA's), but the **data's EZH2 protein is strongly BUFFERED relative to its mRNA** (protein 1.5× vs mRNA 2.2× across the cycle), more than accumulate-and-halve can produce (dilution caps integration at ~1 cycle). 3/4 params rail to bounds chasing it → the misfit is NOT in these rate constants. **KEY CAVEAT: the mRNA target is GNP/MB scRNA-seq (Luo/Ocasio), the protein is MB55 IF — different assays/contexts;** protein-only, a higher `kEZbas` flattens the model to ~1.5 (cf. the GNP-context `sim_ezh2_phase_fit`), so the tension may partly be an inter-dataset mismatch, not pure biology. Top row = per-parameter SSE scatter (identifiability); bottom = best fit vs data + residual table. Pal/RO still excluded (2N-collapse). Related: [[ezh2-baseline-transcription-fix]]. |
| `sim_ezh2_arrest_calib.pdf` | `sim_ezh2_arrest_calib.py` | **Arrest-model calibration vs MB55 phase proportions (DMSO/HU/Palbociclib; RO3306 dropped per JP).** The measured arrests are INCOMPLETE (HU: G2→0.03, G1→0.44; Pal: G0→0.42, S→0.04), so drug interventions are modeled as PARTIAL (runtime `HU` dose + `vmin_fork`; `kPhRbCd`×fraction), with the G0/G1 E2f-threshold calibrated on DMSO (→G0 0.217). **DMSO matches** (G0 0.22✓, S 0.16, G2 0.16; G1 slightly high 0.47 vs 0.38). **HU & Pal CANNOT be reproduced runtime-only — two structural gaps:** (1) the model's S-gate is *active replication* (`aRc>0.05`), so stalled/slowed replication counts as S, whereas the EdU-based data calls stalled cells G1 → model over-populates S under both drugs (HU S 0.51 vs 0.19; Pal S 0.40 vs 0.04); (2) **no stable G1-arrest** — the bistable G0-vs-cycling switch gives Pal G0 0.55 / G1 **0.00** (data G1 0.25); stronger blocks over-collapse to G0 or fail integration. Both need cells that STALL in G1 (committed/2N/pre-replication) — a `build_model_v44` change, not a runtime parameter. Documents the achievable match + the precise blocker for the arrest model. Related: [[ezh2-baseline-transcription-fix]]. |
| `sim_mark_kdeez_ramp_sweep.pdf` | `sim_mark_kdeez_ramp_sweep.py` | **EZH2 half-life (`kDeEZ`) sweep — the H3K27me3 mark's ramp-up/down effects require a SLOW-turnover EZH2.** Motivated by the 25/27 recalibration (kDeEZ t½ 77h→10h) collapsing the mark's dynamic effects in the ramp & developmental experiments. For 7 kDeEZ values (t½ 2–128h) runs ramp-UP (entry) + ramp-DOWN (withdrawal) ensembles WITH (f0_mk=0.145) vs WITHOUT (f0_mk=1.0), GNP, N=50. **Result — a THRESHOLD/switch, not a gradient:** all three mark effects turn ON sharply above **t½ ~25–40h**. **(A)** entry suppression: WITH div-rate ~0.3/day vs WITHOUT ~0.8 at t½≥40h, but WITH≈WITHOUT at t½≤20h. **(B)** withdrawal arrest memory: WITH ~0.4 vs WITHOUT ~0.15 at long t½, converging (~0.15) at short t½. **(D)** hysteresis loop width jumps from ~0 (t½≤20h) to ~0.55 (t½≥40h). **(C) mechanism:** mean mark `Mk` rises monotonically with t½ (0.02→0.32); the dynamic effects switch on once Mk clears a repression threshold (~0.15–0.2), reached only above t½~25h. **The tension, quantified:** the validation/withdrawal-IF data pin EZH2 to t½~10h (Mk~0.09, below threshold → no dynamic mark effect), while the mark-as-slow-integrator / entry-exit-hysteresis / withdrawal-memory story needs t½≳25–40h — **no single half-life satisfies both.** Related: [[ezh2-baseline-transcription-fix]], sim_mark_lifecycle_hysteresis. |
| `sim_mark_period_crossover.pdf` | `sim_mark_period_crossover.py` | **Clean μ-only sweep: the H3K27me3 dilution-vs-restoration race as a smooth curve + the CROSSOVER period.** Controlled version of `sim_withdrawal_mark_dilution` panel D — hold all abundance axes at model defaults, vary ONLY growth `mu` so cell-cycle period is a clean independent variable; constant Hh=1.0; single deterministic cell per point; PASSIVE (f0_mk=1, mark can't repress) vs ACTIVE (f0_mk=0.233). **(A)** mark-vs-period is a clean sigmoid — 0.29 (13 h) → plateau (~0.50 passive / ~0.44 active); **crossover ≈ 16 h** (half-max). Below → dilution wins (low mark, regime 1); above → restoration wins (plateau, regime 2/3). **(B)** ACTIVE CyclinD1 falls 5→3 as period lengthens & the mark builds. **(C) the refinement:** EZH2 (writer) is **non-monotonic** — peaks ~18 h, declines at long period → restoration is NOT constant (E2f/cycle-gated), so the plateau rolls off at very long periods. **(D)** removal decomposition: dilution dominates below ~16 h, turnover sets the plateau above. **Context: the default period (~20–24 h) sits ABOVE the crossover** (restoration-dominated) — only unusually fast cyclers (<16 h) fall into the diluted-low regime. |
| `sim_withdrawal_mark_dilution.pdf` | `sim_withdrawal_mark_dilution.py` | **Hh withdrawal — the H3K27me3 dilution-vs-restoration race + "is transient G0 more or less WITH the mark?"** Adds a **cell-cycle-duration (mu) distribution** (LogN(0,0.22); the pivot of the dilution race) on top of the calibrated abundance distributions + partition noise + Dna-reset detection. N=500/condition. **(A) transient-G0 is LESS with the mark** during withdrawal (~0.07–0.12 occupancy at low Hh vs ~0.18–0.20 WITHOUT; converging at high Hh) — **(B)** because it converts to deep ARREST (WITH 0.28–0.40 vs WITHOUT 0.13–0.16 at low Hh); the mark deepens quiescence (total quiescence higher WITH, but arrest not transient). **(C) refines the regime model:** the mean mark FALLS as Hh withdraws and is LOWER with the mark — because *restoration is cycle-gated* (EZH2 writer is E2f-gated → collapses as cells slow/arrest), so the mark can't self-maintain the arrested state; its causal work is at the transition (tipping slowing cells into arrest), then it fades. **(D) the race:** per-cell mark vs cell-cycle period at high Hh — a real positive trend (short period → diluted-low mark; long period → plateau, regimes 1 vs 2/3) but heavily scattered by EZH2-abundance co-variation, so period alone doesn't set the mark. Caveat: mu-CV is not GNP/MB-calibrated (~0.2 plausible). |
| `sim_mark_lifecycle_hysteresis.pdf` | `sim_mark_lifecycle_hysteresis.py` | **Ramp-UP (entry) + STEADY-STATE + ramp-DOWN (withdrawal), WITH vs WITHOUT H3K27me3 repression of CyclinD1 — the three experiments re-run with the UPGRADED machinery** (calibrated abundance distributions: CyclinD1 CV 0.70 / birth-p27 CV 0.33 / EZH2 CV 0.55; division-inheritance partition noise on birth-p27; **Dna-reset division detection**; random cell-cycle phase per lineage to de-synchronize division waves). N=2000/condition, parallel. Readout = **division rate (per cell per day) vs the instantaneous Hh** — a clean proliferation metric (the naive "1−arrest" fails for entry, since a cell that starts quiescent but enters later is never *terminally* arrested). **(A) entry:** WITH mark suppresses proliferation at low-mid Hh (dips to ~0.3/day vs ~1.0 WITHOUT), converging by SHH≈0.65. **(B) steady:** WITH mark lower at low Hh (0.53 vs 0.89/day), converging at high Hh (~1.0/day = 24 h period). **(C) withdrawal:** WITH mark modestly lower; cells coast. **(D) hysteresis synthesis:** at low Hh, entry (green, lags LOW ~0.3) < steady (~0.53) < withdrawal (red, coasts HIGH ~0.7) — the classic entry/exit loop — and the mark **widens** it. So the mark's dominant effect is on ENTRY (harder to start) more than exit, and it sharpens the Hh-dependence of proliferation. Uses only the sim/analysis layer (model file untouched); see [[v44-deterministic-multiperiodic-g0]]. **`--sdmu=0.22` companion (`sim_mark_lifecycle_hysteresis_mu0.22.pdf`)** adds a growth-time (cell-cycle-duration) distribution: entry & steady mark-suppression are preserved, but the ramp-DOWN division rate goes FLAT (~1/day, WITH≈WITHOUT) — because a growth-time spread creates fast cyclers *below the ~16 h dilution crossover* (`sim_mark_period_crossover`) that carry a low mark, escape repression, and coast; being fastest they dominate the division-weighted rate. The mark's withdrawal effect is then only visible in the ARREST occupancy (dotted overlay: WITH ~0.33–0.40 vs WITHOUT ~0.15–0.19 at low Hh) — the slow high-mark cells still arrest. |
| `sim_inheritance_partition.pdf` | `sim_inheritance_partition.py` | **PROTOTYPE: data-calibrated abundance distributions + real division-to-division inheritance with PARTITIONING NOISE** (N=200/level/condition, parallel). Two upgrades over the placeholder ensembles: (1) calibrated per-lineage draws (shape/CV only) — `k_Cd_translation=0.801·LogN(0,0.633)` (CyclinD1 CV 0.70), `P21_div=0.6·LogN(0,0.32)` (birth-p27 CV 0.33, unimodal), `kTlEZ=0.004·LogN(0,0.51)` (EZH2 abundance CV ~0.55); (2) at each division the birth-p27 is REDRAWN `= P21_div·exp(N(-s²/2,s))` (mean-preserving, s=0.30) instead of a fixed lineage value — injected in the sim layer, model file untouched. **(A/B) composition** (immediate/transient/arrest) vs SHH, WITH vs WITHOUT mark, no-noise vs noise: WITHOUT-mark cells barely arrest; WITH-mark arrests the low-CyclinD1 tail (0.42→0.09 as SHH 0.4→1.0); partition noise leaves the population fractions ~unchanged (mean-preserving). **(C/D) the key correction:** near the commitment threshold a **deterministic** lineage does NOT have one fixed fate — it recurs through transient G0 on a **regular multi-periodic schedule** (C); partition noise makes that recurrence **irregular/stochastic** so sister divisions diverge (D). **Bug fixed:** divisions detected via the Dna 1→0 reset (canonical, [build line 556]), NOT MPF peaks — MPF spikes are too sharp for the coarse chunk sampling and were undercounted → spurious arrest. **Prototype caveats:** birth-p27 median (0.6→0.42 operating point) is a.u./uncalibrated and dominates the absolute arrest fraction; the partition σ is a chosen value (no data on p27 partition asymmetry); CyclinD1 is NOT given partition noise because in v44 it's mitogen-clamped (a division perturbation relaxes back before the fate decision) — the fate-moving inheritance noise is on p27. |
| `sim_steadystate_g0_mark.pdf` | `sim_steadystate_g0_mark.py` | **STEADY-STATE (constant Hh) transient-G0 & arrest, REPRESSED vs NOT repressed** (N=600/level/condition, parallel) — disentangles the withdrawal observation "fewer transient-G0 WITH mark" from its arrest confound. Same heterogeneity draws as the withdrawal sim; each cell settles into a limit cycle at fixed SHH and is classified (probe_transient_g0 conventions): <2 divisions=ARREST, cycling with p27-dwell ≥2 h=TRANSIENT G0, <2 h=IMMEDIATE re-entry. **Result — the mark makes cells more quiescence-prone at ALL Hh (immediate re-entry ~0.16–0.23 WITH vs ~0.25–0.30 WITHOUT), but WHERE that quiescence goes is Hh-dependent:** (A) transient-G0 fraction — the two conditions **cross at ~SHH 0.57**: at LOW Hh the mark gives *fewer* transient-G0 (0.48 vs 0.75 at 0.30), at HIGH Hh *more* (0.76 vs 0.70 at 1.0). (B) the missing transient-G0 at low Hh reappears as deep ARREST (WITH 0.35→0 across SHH; WITHOUT ~0 throughout). (C) full immediate/transient/arrest composition. (D) mechanism: the mark holds CyclinD1 ~2× lower at every Hh (lower CyclinD1/p27 ratio) and the surviving transient-G0 cells dwell *longer*. **So the withdrawal appearance ("fewer transient-G0 WITH mark") is the low-Hh regime showing through — real, but it's transient-G0→ARREST conversion (deeper quiescence), not fewer quiescent cells; total quiescence is higher WITH the mark everywhere.** |
| `sim_hh_entry_threshold.pdf` / `sim_hh_withdrawal_threshold.pdf` | `sim_hh_division_thresholds.py` | **The Hh threshold for cell division, REPRESSED (f0_mk=0.233) vs NOT repressed (f0_mk=1.0) — entry & withdrawal as two focused figures.** Each shows only the CyclinD1 context curve, the individual division events (rug at each event's Hh), and the Hh threshold (lowest-Hh division) per condition. **Entry (ramp up from arrest):** repression DELAYS the start — cells begin dividing at Hh 0.32 vs 0.16 (ΔHh 0.15), because the mark accumulated during arrest represses CyclinD1 and must be overcome. Robust (independent of dwell). **Withdrawal (from established high-Hh cycling):** repression makes cells STOP SOONER — Hh 0.48 vs 0.15 (ΔHh 0.33): a cell established at high Hh with the mark on has CyclinD1 capped (~3.7 vs ~7.4) by the EZH2 feedback, so withdrawal crosses the division-off threshold with less buffer. **Caveat:** the withdrawal ΔHh is history/dwell-dependent — it needs the cell to have been established long enough at high Hh for the mark to load; a cell only briefly at high Hh shows a much smaller exit gap (triangle trace: 0.087 vs 0.073). Net (GNP context): the H3K27me3 feedback NARROWS the proliferative Hh window from both sides (harder to start, quicker to stop) = tighter negative-feedback tracking of Hh; MYCN in MB would floor CyclinD1 and blunt it. |
| `sim_ramp_hysteresis_trace_control.pdf` | `sim_ramp_hysteresis_trace_control.py` | **Triangle-ramp trace, WITH vs WITHOUT H3K27me3 repression on CyclinD1** (f0_mk=0.233 vs 1.0; mark still runs in both, only its effect on Cd is cut). (A) shared SHH triangle. (B) CyclinD1 cycle-avg: WITH is gated & lags, WITHOUT higher — but only *slightly* in this fast transient (the mark stays dilution-low while cycling; the ~2× repression seen at steady state is masked). (C) the mark accumulates in arrest & dilutes with divisions in BOTH, but only WITH feeds back on Cd; WITHOUT has fewer divisions ⇒ less dilution ⇒ higher mark sawtooth. (D) input-output: **WITH entry 0.32 / exit 0.09 (loop); WITHOUT entry 0.16 / exit 0.07 (nearly retraces).** So the mark supplies the entry-delay & the loop; a small residual loop WITHOUT is the Rb-E2f/Skp2-p27 switch. NB steady-state check (constant SHH): mark represses Cd ~2× (e.g. 3.4 vs 7.4 at SHH 1.0) but shifts cycle period only ~15% (~22 vs ~23 h) — CyclinD1 is not the rate-limiting period-setter, so the triangle's raw 26-vs-13 division count overstates the effect (transient + coarse mass-drop detection). Caches both runs for the phase-plot figure. |
| `sim_ramp_hysteresis_phaseplot.pdf` | `sim_ramp_hysteresis_phaseplot.py` | **Phase-plane portraits of the triangle-ramp trajectory (state vs state), WITH vs WITHOUT repression** — reuses the trace_control cache. (A) CyclinD1 vs H3K27me3 mark, WITH: a **negative-feedback manifold** (arrest corner = high mark/low Cd; cycling corner = diluted-low mark/high Cd), with the up- & down-legs enclosing area; faint grey = the fast cell-cycle limit cycles drifting along the slow manifold (2-timescale portrait). (B) same axes, WITHOUT: the feedback arrow is cut, so CyclinD1 & the mark merely **co-drift with SHH and the slope flips POSITIVE** — the clean signature that the coupling is gone. (C) CyclinD1 vs SHH input-output loops for both (WITH opens a loop, WITHOUT nearly retraces). |
| `sim_ramp_hysteresis_mark_control.pdf` | `sim_ramp_hysteresis_mark_control.py` | **WITH-vs-WITHOUT-repression control for the hysteresis area — is the loop caused by the mark?** Identical model at default timing, two arms: mark represses CyclinD1 (`f0_mk=0.233`) vs mark neutralized (`f0_mk=1`, so R=1 for all Mk — the mark still accumulates/dilutes and EZH2 runs, but exerts ZERO effect on CyclinD1). **(A)** the CyclinD1–SHH loops at a fast ramp: WITH-mark = wide gold loop (area ~0.33); NO-repression = up/down curves collapse together (area ~0.02) and sit *higher* (unrepressed). **(B)** loop area vs ramp speed: mark-ON ~0.33 and **flat** across all speeds; mark-OFF ~0.05 at the fastest ramp **decaying to 0** by ~100 h. **Conclusion: the hysteresis is essentially all H3K27me3** — mark contributes >85 % of the loop at any speed and ~100 % of the *persistent* loop. The residual mark-OFF loop is the cell-cycle commitment switch (Rb-E2f/Skp2-p27), which behaves textbook (closes quasi-statically); the mark's loop does NOT close even at a 400 h ramp because the arrest-accumulated mark can only be cleared by *division-driven dilution* on re-entry — an epigenetic memory that survives the quasi-static limit where the switch memory doesn't. |
| `sim_ramp_traces_diagnostic.pdf` | `sim_ramp_traces_diagnostic.py` | **Trace diagnostic** — complete trajectories (SHH, CyclinD1, H3K27me3 mark, divisions) for the 4 corners of (H3K27me3 strength × cell-cycle duration), entry & withdrawal ramps. Shows the mechanism directly: short cycle keeps the mark diluted/low (sawtooth halving), long cycle accumulates it; strong mark + long cycle can accumulate enough to collapse CyclinD1 and stall cycling (and is numerically stiff). Confirms the "first-division-during-ramp" metric is unreliable (residual early divisions) → the bisection/threshold metrics are the right ones. |

---

## Calibration / diagnostic scripts (no figure)

| Script | Purpose |
|---|---|
| `validate_v44.py` | Unified validation harness: all experimental‑target checks + figure‑critical behaviors (run this to score the model). |
| `v44_calibrate_hh.py`, `v44_optimize_hh.py` | HH/MYCN→CyclinD1 between‑condition ratio calibration. |
| `v44_calibrate_ezh2.py` | EZH2 within‑cycle gradient + HU boost calibration. |
| `v44_calibrate_final.py`, `v44_probe_sets.py` | constrained whole‑model parameter search (figure‑safe guard). |
| `probe_transient_g0_v2.py`, `analyze_cyclind1_g0.py` | transient‑G0 vs CyclinD1 probes (p27 marker). |
| `probe_two_step.py` | two‑step‑Rb variant probes (`with_two_step_rb=True`). |

---

## v45 figures — ARCHIVED

The v45 stochastic-commitment exploration (model, validator, and its 10-figure suite) has been
moved to **`archive/v45/`** (see `archive/v45/README.md`). **v44 is the committed working model**;
its figures are the ones listed above.
