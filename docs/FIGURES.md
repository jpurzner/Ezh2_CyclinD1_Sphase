# Model figures — which code generates each

All scripts live in `simulations/` and run against the calibrated **v44 single-step default**
(`src/build_model_v44_heldt.py`). Each writes a `.png` (preview) and a `.pdf` (vector, for the paper)
into `simulations/`. Regenerate any single figure with, e.g.:

```bash
./venv/bin/python simulations/fig_v44_fig5_rescue.py
```

**After any model change, regenerate the whole set with the manifest** (auto-globs every
`fig_v44_*.py` and explicitly runs the slow ensemble figures that don't match that glob, e.g. the
cyclin D1 / birth-p27 bifurcation):

```bash
bash simulations/regenerate_figures.sh
```

The unified validation harness `simulations/validate_v44.py` defines the conditions, the division
counter, the phase (p27) classifier, and the experimental-target checks that the figure scripts import.

---

## Paper figures (model)

| Manuscript panel(s) | Output PDF | Generating script | Notes |
|---|---|---|---|
| **Fig. 5A–C** — Ptch+/− MB / +HHi (arrest) / +HHi+Ezh2i (rescue), Cyclin B traces | `fig_v44_fig5_rescue.pdf` | `fig_v44_fig5_rescue.py` | |
| **Supp. Fig. 7A** — model architecture / wiring diagram | `fig_v44_wiring.pdf` | `fig_v44_wiring.py` | pure schematic (no simulation) |
| **push/pull summary schematic** — what drives EZH2 & CyclinD1 up/down + the H3K27me3 feedback | `fig_v44_pushpull_schematic.svg` | hand-authored SVG (no simulation) | Balance-sheet capstone for the EZH2↔CyclinD1 thread: green = drives up, red = pushes down; EZH2→H3K27me3⊣CyclinD1 leaky-rheostat feedback; three emergent-behavior callouts (progressive repression/buffering; proliferation-rate sets the brake via dilution ∝1/period; **G0 reversible/monostable, not a bistable lock**). Vector; converts to PDF for the manuscript (rsvg-convert/Inkscape). |
| **Supp. Fig. 7B–M** — validation grid (Cyclin B traces; CyclinD1/Mycn/Gli1 model‑vs‑data; EZH2; division counts; time courses) | `fig_v44_validation.pdf` | `fig_v44_validation.py` | |
| **Supp. Fig. 8A–H** — EZH2→CyclinD1 feedback (±feedback: Cyclin B, CyclinD1 mRNA, EZH2 protein, phase relationship) | `fig_v44_feedback.pdf` | `fig_v44_feedback.py` | feedback OFF = `f0_mk = 1.0` (AUM mark cannot repress) |
| **Supp. Fig. 8I,J** — MB+CDK4/6i and MB+CDK4/6i+Ezh2i (no rescue) | `fig_v44_cdk46i_norescue.pdf` | `fig_v44_fig5_rescue.py` | emitted alongside Fig. 5 |
| **Supp. Fig. 7x** — Gli→Ptch1 negative feedback & its disruption in MB (Gli1 step overshoot GNP vs MB; Ptch1 mRNA induction; Smo/Gli broken‑loop) | `fig_v44_ptch1_feedback.pdf` | `fig_v44_ptch1_feedback.py` | Ptch1 = Gli target; `Ptch1_copy_number` = functional fraction (1.0 GNP / 0.1 MB) |
| **Fig. 5C** — population cycling fraction (vismo↓, Ezh2i restores; CDK4/6i no rescue) | `fig_v44_fig5_population.pdf` | `fig_v44_fig5_population.py` | N=120 ensemble, CyclinD1/p27 heterogeneity |
| **Conditions → input parameters + parameter space** (A: input-knob matrix per condition; B: CyclinD1×p16 plane with cycle/arrest boundary + each condition placed) | `fig_v44_condition_paramspace.pdf` | `fig_v44_condition_paramspace.py` | shows MB+HHi arresting at the *same* CyclinD1 a GNP cycles at, because p16 raises the threshold; EZH2i crosses it back |

Drop‑in replacement Results/Methods/captions for the manuscript: `simulations/PAPER_MODEL_SECTIONS_v44.md`.
Data sources used to parameterize the model: `docs/PARAMETERIZATION.md`.

---

## Exploratory analyses (developed while interrogating the model; not current manuscript figures)

| Output PDF | Generating script | What it shows |
|---|---|---|
| `fig_v44_ezh2i_doseresponse.pdf` | `fig_v44_ezh2i_doseresponse.py` | Graded EZH2i dose‑response of CyclinD1 (transcript+protein) and proliferation in GNP / MB / MB+HHi / MB+CDK4/6i — the rescue **threshold** and the no‑rescue control. |
| `fig_v44_ezh2i_population_dose.pdf` | `sim_ezh2i_population_dose.py` | Population EZH2i dose‑response: % of a heterogeneous (cyclin D1/p27) tumour pushed back into cycle vs dose — the "low‑dose rescues a fraction" curve. (~15 min) |
| `fig_v44_feedback_role.pdf` | `fig_v44_feedback_role.py` | WITH vs WITHOUT the EZH2 ⊣ CyclinD1 arm across mitogen: the feedback installs a mitogen‑gated quiescence threshold (cell‑cycle exit). |
| `fig_v44_ckoo_feedback_strength.pdf` | `fig_v44_ckoo_feedback_strength.py` | Is the ~3× CyclinD1 supported by the cKO? Recombination correction of the bulk fold + model feedback‑strength sweep. |
| `fig_v44_g0_mechanism.pdf` | `fig_v44_g0_mechanism.py` | CyclinD1 sets transient‑G0 duration (p27 marker); EZH2i collapses it; arrest below threshold. |
| `fig_v44_cycle_anatomy.pdf` | `fig_v44_cycle_anatomy.py` | One‑cycle anatomy: the transient G0 is the p27‑high window (phospho‑Rb saturates early — why we classify G0 by p27). |
| `fig_v44_g0_bifurcation.pdf` | `sim_g0_bifurcation.py` | Proliferation–quiescence bifurcation from cyclin D1 / **birth-p27** (`P21_div`) heterogeneity (N=140). Low birth p27 → immediate re-entry, high → prolonged transient G0 (the ultrasensitive Fan–Meyer threshold; mapped by `probe_birthp27_dwell.py`); low CyclinD1 → arrest. SHH and EZH2i tune the split (immediate ↑, arrest ↓, dwell ↓). On the baked model. (~10 min) |
| `fig_v44_mitogen_withdrawal.pdf` | `sim_mitogen_withdrawal.py` | **Mitogen-withdrawal experiment on the heterogeneous GNP ensemble (N=16, birth-p27 + CyclinD1-setpoint draws).** Cells equilibrated cycling at high SHH, then mitogen withdrawn SUDDENLY or GRADUALLY to a range of final levels; counts post-withdrawal divisions ± the EZH2⊣CyclinD1 feedback (no-feedback = `f0_mk`→1.0, the H3K27me3 mark cannot repress). (A/B) divisions vs final mitogen, sudden/gradual — the **feedback gap is depth-dependent, maximal at partial withdrawal (SHH 0.15–0.3)** and closes at full withdrawal (both arrest) and at baseline (both cycle); sudden > gradual. (C) per-cell distributions at SHH→0.3: sudden +fb 2.4 vs −fb 4.6; gradual +fb 3.3 vs −fb 4.5. (D) representative cell: no-feedback CyclinD1 stays high → coasts (5 div), feedback represses CyclinD1 → arrests (1 div). State-reuse + tol-retry; `--probe` / `--fresh`. (~2.5 h) |
| `fig_v44_ezh2i_rescue_kinetics.pdf` | `fig_v44_ezh2i_rescue_kinetics.py` | **EZH2i fast-tracks CyclinD1 de-repression → rescues vismo-arrested MB** (uses the calibrated `with_h3k27_memory` model — an explicit H3K27me3 mark at the CyclinD1 locus). Vismo lowers Gli-driven CyclinD1 but the MYCN floor keeps it near threshold; residual H3K27me3 holds it just below, and EZH2i clears the mark to re-tip it. EZH2i blocks deposition → mark cleared by demethylation + replication-dilution → CyclinD1 de-represses over ~24–48h → cycle re-entry. (A) CyclinD1 ± EZH2i; (B) the H3K27me3 mark (mechanism); (C) timed dosing @0/24/48/72h — de-represses whenever given (the fast-track); (D) rescue outcome (5–6 div vs vismo-only's 1) on-demand. **NB (2026-07): the earlier "EZH2-maintained lock-in" framing is superseded** — EZH2 is cell-cycle-gated (falls in G0) and the mark does NOT make arrest bistable (G0 is monostable/reversible, "sticky"/metastable — see `fig_v44_g0_bistability_confirm`); the rescue itself (EZH2i re-tips the MYCN-floored, mark-repressed borderline) is unchanged. `--fresh`. (~15 min) |
| `fig_v44_vismo_withdrawal.pdf` | `sim_vismo_withdrawal.py` | **Vismodegib treatment of the heterogeneous MB ensemble — the MB analog of mitogen withdrawal.** Vismo (GDC0449) blocks Smo, removing Gli-driven CyclinD1 but leaving the MYCN floor → even full vismo is a *partial* withdrawal, so the EZH2 feedback is decisive (= the model's vismo-arrest / EZH2i-rescue). MB cells equilibrated cycling, then vismo applied SUDDENLY/GRADUALLY to a range of doses; counts post-treatment divisions ± feedback (no-fb = `f0_mk`→1.0 ≡ EZH2i/tazemetostat). (A/B) divisions vs dose — feedback gap opens at high vismo. (C) at near-full vismo (GDC=0.95): +fb → uniform arrest (1 div); −fb (EZH2i) → **bimodal rescue** (high-CyclinD1-setpoint cells coast 4–5, low ones arrest = "rescues a fraction"). (D) representative cell: no-fb MYCN-floor CyclinD1 sustains cycling, fb represses → arrest. Capped at GDC=0.95 (GDC=1.0 = Smo singularity, spurious arrest; vismo never 100%-blocks Smo). State-reuse + tol-retry; `--probe`/`--fresh`. (~2.5 h) |
| `fig_v44_ezh2_phaseplane.pdf` | `sim_ezh2_phaseplane.py` | **Ferrell-style phase-plane / bifurcation analysis of the EZH2→H3K27me3 ⊣ CyclinD1 feedback (AUM model).** In the AUM model the direct repressor is the H3K27me3 mark Mk, so the plane is **(Mk, CyclinD1)**. (A) Mk–CyclinD1 nullcline portrait (GNP): Cd-nullcline analytic (Cd fast, mark represses it via `R(Mk)=f0+(1−f0)/(1+(Mk/K)^n)` ↓), Mk-nullcline traced by breaking repression (`f0_mk=1`, sweep drive) so Mk is a readout of Cd (↑); they meet at one stable node (monostable, buffering); fast/slow direction field + limit-cycle orbit overlaid. (B) GNP/MB/+EZH2i — mitogen drive & feedback removal move the operating point. (C) bifurcation vs CyclinD1 drive: feedback shifts the proliferation threshold higher (mean-E2F order parameter). (D) bifurcation vs mitogen (Gli1): feedback raises the mitogen threshold for proliferation (gated quiescence). Two-stage npz cache; `--fresh`. (~35 min) |
| `fig_v44_replicative_dilution.pdf` | `fig_v44_replicative_dilution.py` | **Replicative dilution of H3K27me3 limits EZH2 repression — mitogen sweep, dilution ON vs OFF (AUM model).** Compares the default (S-phase halving `Mk=0.5*Mk`) against the halving disabled (`Mk=1.0*Mk`) across a GNP SHH sweep. (A) Mk(t) at SHH=0.8: ON = sawtooth (halved each division, re-methylates), OFF = smooth & higher — the dilution events visualized. (B) steady Mk vs mitogen: dilution holds Mk below the no-dilution ceiling and **the gap widens with mitogen** (faster cycle → more-frequent dilution; gap 0.075→0.16). (C) steady CyclinD1 vs mitogen: the mark relief → higher CyclinD1 (EZH2 reach limited by proliferation rate). (D) divisions/168h vs mitogen = the dilution frequency (the driver). No cache (~3–5 min). |
| `fig_v44_h3k27_response.pdf` | `fig_v44_h3k27_response.py` | **H3K27me3 → CyclinD1 response characteristics — static transfer functions + parameter sensitivity (algebraic steady state, fast).** (A) leaky‑Hill readout `R(Mk)=f0+(1−f0)/(1+(Mk/K)^n)` — the mark throttles toward a floor `f0=0.233`, never silences; steepness set by `n_mk`. (B) methylation dose‑response `Mk_ss(EZH2)` — read‑write autocatalysis (`k_w_mk`) sharpens it, and **faster cycling dilutes it down** (arrested vs 34 h vs 18 h period). (C) the throttle `R(Mk_ss(EZH2))` — transcript falls away from the no‑brake drive as EZH2 rises (the buffering gap). (D–I) per‑parameter small‑multiples (0.5×/1×/2×): `del_mk`, `K_mk`, `k_w_mk` dominate the response; `f0_mk` sets the high‑EZH2 tail floor; `g_mk` (transcription eviction) is nearly inert at the calibrated weak regime. Transcription‑eviction held at a representative `Cd_mRNA`. |
| `fig_v44_h3k27_timing.pdf` | `fig_v44_h3k27_response.py` | **H3K27me3 → CyclinD1 timing & cell‑cycle impact (full v44 model).** (A) de‑repression kinetics after EZH2i — mark half‑life **t½ ≈ 15 h**, tunable ~2× by `del_mk`; smooth demethylation + step‑drops at each division. (B) replicative‑dilution **sawtooth** — Mk halved every S phase then rebuilds, clocked by the cycle (CyclinD1 overlaid). (C) **mean Mk vs cell‑cycle period** (growth‑rate sweep) — period 12→38 h ⇒ mean Mk 0.25→0.46 and mean transcript inversely (~7→3.5): faster proliferation out‑dilutes the writer, a positive feedback on cycling speed. (D) entry timing on a rising‑Hh ramp — the brake delays first division from **29 h / SHH 0.23** (EZH2i) to **50 h / SHH 0.35** (default). Emitted by the same script as `_response`; ~1–2 min. |
| `fig_v44_cellcycle_duration.pdf` | `fig_v44_cellcycle_duration.py` | **Cell‑cycle duration / growth phase sets the H3K27me3 brake (replicative‑dilution frequency = 1/period).** (A) mark sawtooth at a short (13 h) vs long (35 h) cycle — the longer cycle rebuilds more mark between S‑phase halvings (⟨Mk⟩ 0.27→0.46). (B) dilution ON vs OFF vs period: the "dilution relief" gap shrinks from 0.26 (~10 h) to 0.05 (~54 h) — the impact of replicative dilution diminishes as duration increases. (C) **developmental slowing** — growth rate ramped down → cycle lengthens → mark accumulates → CyclinD1 progressively repressed toward exit (a feed‑forward, NOT a self‑slowing loop: CyclinD1 doesn't set the running period in v44). (D) mean CyclinD1 transcript & division rate vs period. ~2 min. |
| `fig_v44_g0_mark_dynamics.pdf` | `fig_v44_g0_mark_dynamics.py` | **CONSOLIDATED — H3K27me3 dynamics in arrested G0: plateaus, a race, reversible.** (A) arrest time‑course: the mark does NOT run away — it overshoots (dilution stops) then settles to a modest plateau (~0.31, *below* the cycling mean) because the writer EZH2 is E2f‑gated and collapses in G0. (B) phase diagram (reads the `g0_mark_regime` cache): whether the arrested mark ends up above/below cycling is a RACE between EZH2‑writer persistence (half‑life) and mark turnover; the measured EZH2 t½≈77 h places GNPs on the RELAX (reversible) side, near the fence. (C) two‑initial‑condition test (reads the `g0_bistability_confirm` cache): from a high‑mark or low‑mark start the cell settles to the SAME state at every drive → **MONOSTABLE, reversible** (a sharp but single‑valued transition; "sticky"/metastable, NOT a bistable lock). Pulls the two caches + regenerates panel A (~1 min). |
| `fig_v44_g0_mark_regime.pdf` | `fig_v44_g0_mark_regime.py` | **Phase diagram: does H3K27me3 accumulate or relax in persistent G0 arrest?** Sweeps EZH2 protein half‑life (`kDeEZ`; measured G2/G0 = 1.48× IF → ~77 h, plausible range 46–580 h) × mark turnover half‑life (`del_mk`); at each, simulates a cycling (SHH=1.0) and a long‑arrested (SHH=0.1, no dilution) cell and colors (arrested − cycling) mean mark. **RELAX** (writer collapse wins → reversible) vs **ACCUMULATE** (dilution‑loss wins → mark ratchets), split by the exact‑tie contour. Operating point sits in RELAX near the boundary; the measured EZH2‑half‑life box STRADDLES the boundary → the direction is decided by the (G0‑specific) EZH2 stability — the measurement to make. 16×16 grid, cached (~15 min). |
| `fig_v44_g0_bistability_confirm.pdf` | `fig_v44_g0_bistability_confirm.py` | **Definitive bistability test — fixed‑SHH, two‑initial‑condition equilibration.** At each SHH the cell is dropped in from BOTH a high‑mark (arrested) and a low‑mark (cycling) start and integrated to TRUE steady state (stiff‑zone tolerance escalation). In BOTH the calibrated (RELAX) and ACCUMULATE regimes the two starts CONVERGE → **MONOSTABLE**; the mark does NOT create a bistable G0 lock. The apparent hysteresis of the quasi‑static sweep (`fig_v44_g0_hysteresis`, superseded) was a slow‑transient artifact. Conclusion: G0 is reversible/monostable; stable EZH2 + slow mark make arrest **metastable** ("sticky", resolves over hundreds of h), not latched. Cached (~15 min). |
| `fig_v44_g0_hysteresis.pdf` | `fig_v44_g0_hysteresis.py` | **SUPERSEDED** by `fig_v44_g0_bistability_confirm` — a quasi‑static SHH sweep that FALSE‑POSITIVED bistability in the ACCUMULATE regime (the ~230 h‑half‑life mark can't equilibrate in a ~200 h/step sweep, so the arrested branch *looks* locked but is a long transient). Kept as a methodological cautionary record; do not cite as bistability evidence. |
| `fig_v44_paramsweep_{entry,withdrawal,cumprolif,cuts}.pdf` | `sweep_param_exploration.py` → `plot_param_exploration.py` | **HIGH-RESOLUTION parameter sweep** (50×50×10 = 2,500 entry thresholds + 25,000 withdrawal ramps; cached in `sweep_param_exploration_cache.npz`; ~2 h, NOT in the auto-regen). `_entry`: fine mitogen-threshold-for-first-division heatmap over (EZH2 responsiveness `kEZE2f` × inhibition depth `1−f0`) with contours + the no-proliferation (NaN) region. `_withdrawal`: 2×10 small-multiples — exit-Hh (smooth) and withdrawal-division (discrete) heatmaps, one column per Hh decline rate (50→500 h). `_cumprolif`: **cumulative proliferation = fold expansion 2^divisions** (symmetric GNP divisions; median-smoothed, log color), per decline rate — spans 1× (fast decline / strong EZH2) to ~10³–10⁴× (slow decline / weak-unresponsive EZH2). `_cuts`: 1-D line cuts through the default (entry threshold + cumulative proliferation). Re-plot anytime from the cache via `plot_param_exploration.py`. |
| `fig_v44_param_exploration.pdf` | `fig_v44_param_exploration.py` | **Parameter exploration (coarse, superseded by the sweep above): EZH2 mitogen-responsiveness (`kEZE2f`) × CyclinD1 inhibition depth (`1−f0_mk`) × Hh decline rate → entry threshold & withdrawal divisions.** (A) mitogen threshold for first division over (responsiveness × inhibition) — both knobs raise the threshold monotonically, spanning SHH 0.11 (weak) → 1.99 (strong), default ~0.34. (B) divisions during a 150 h Hh withdrawal over the same plane — stronger/more-responsive EZH2 → fewer divisions (0 at strong EZH2). (C) withdrawal divisions vs Hh decline rate for 4 settings — slower decline → more divisions; strong inhibition caps at 0, no-EZH2 gives the most. Entry threshold by SHH bisection; withdrawal divisions are discrete counts (trends robust, exact values noisy). Cached (`--fresh`); ~5 min. |
| `fig_v44_progressive_repression.pdf` | `fig_v44_progressive_repression.py` | **The core consequence: EZH2 is mitogen-responsive → progressive H3K27me3 repression of CyclinD1.** GNP Hh (SHH) dose sweep, steady state. (A) the brake tracks the gas — Gli1 (Hh activity) AND EZH2 + H3K27me3 both rise with Hh. (B) CyclinD1 raw drive (Gli+MYCN, un-repressed = Cd_mRNA/R) rises 5.7× while the actual transcript rises only 2.5×; the shaded gap = amount repressed by H3K27me3, growing with Hh. (C) surviving fraction R = transcript/drive falls **80% → 35%** across the Hh range — the repression is *progressive*. (D) **Hh needed to INITIATE proliferation** — division rate vs Hh with vs without EZH2 repression: EZH2 raises the proliferation threshold from ≤0.08 (basal drive suffices) to SHH ≈ 0.35 — a mitogen gatekeeper. Steady-state (clean, no cell-cycle noise). |
| `fig_v44_cellcycle_exit.pdf` | `fig_v44_cellcycle_exit.py` | **Linear Hh WITHDRAWAL — when the cell cycle ends, with vs without EZH2 repression.** SHH ramped 1.0→0.05 over ~150 h from a cycling start; default vs EZH2i. (A) cycle-avg CyclinD1 vs time with the commitment threshold; EZH2 keeps CyclinD1 lower so it exits **earlier, at higher Hh (SHH≈0.54)** while EZH2-inhibited cells cycle down to **SHH≈0.18** — mirror-symmetric with the entry thresholds (~0.55 vs ~0.20). Exit scored as cycle-avg CyclinD1 dropping below a nominal commitment threshold (2.0 a.u.); "last division" is a noisier proxy. (B) epigenetic state: as Hh withdraws EZH2 **falls** (cycle exit → E2F down), so the GNP arrest is **reversible** — the mark is not sustained the way MB (MYCN-sustained EZH2) locks it in (the EZH2i-rescue contrast). Cached (`--fresh`); ~3 min. |
| `fig_v44_cellcycle_entry.pdf` | `fig_v44_cellcycle_entry.py` | **When a GNP enters the cell cycle under gradually rising Hh — with vs without EZH2 repression of CyclinD1.** SHH ramped 0→1.0 over ~150 h from an arrested start; default (EZH2 active) vs EZH2 inhibited (EZH2i=1). (A) CyclinD1 (cycle-avg) + rising SHH; cell-cycle-entry marker (first division) for each. (B) cumulative divisions — the lift-off = entry. EZH2-inhibited cells commit early (**t≈50 h, SHH≈0.22**); with EZH2 active, commitment is delayed to a higher Hh (**t≈106 h, SHH≈0.59**) — EZH2 gates cell-cycle entry to a genuine Hh threshold (~2.7× more Hh, ~56 h later). Cached (`--fresh`); ~3 min. |
| `fig_v44_progressive_repression_timecourse.pdf` | `fig_v44_progressive_repression_timecourse.py` | **Time-course of progressive repression: a GNP under GRADUALLY rising Hh.** SHH ramped 0.2→2.0 over ~180 h; cycle-averaged. (A) Gli1 (gas) tracks Hh quickly while the slow, mitogen-responsive EZH2→H3K27me3 brake rises behind it (the lag). (B) the CyclinD1 drive climbs 5.7× but the actual transcript only ~4×; the surviving fraction R transiently *rises* early (gas outpaces the slow brake) then *progressively falls* (0.8→0.36) as H3K27me3 accumulates — the repression developing in real time. Cached (`--fresh`); ~3 min. Companion to the steady-state `fig_v44_progressive_repression`. |
| `fig_v44_dilution_tension.pdf` | `sim_ezh2_dilution_tension.py` | **The H3K27me3 tension: EZH2-driven methylation (integrates S-phase + mitogen) vs replicative dilution → CyclinD1 transcript.** (A) Cd_mRNA over (mitogen SHH × S-phase duration) — the physiological tension surface (long-S+low-mitogen → mark wins → Cd low; short-S+high-mitogen → dilution wins → Cd high). (B) S-phase-duration sweep: EZH2↑, mark↑, Cd_mRNA↓ (EZH2 integrates S-phase). (C) control-parameter sweeps (×default): deposition knobs `k_w_mk`/`kEZE2f` push Cd_mRNA **down**, removal knobs `del_mk`/`kDeEZ` push it **up**. (D) k_w_mk sweep, dilution ON vs OFF (gap = dilution relief). Cached; `--fresh`. (~10 min) |
| `fig_v44_dilution_tension_variants.pdf` | `sim_ezh2_dilution_tension_variants.py` | **HU-axis + MB/vismo variants of the tension surface.** (A) HU-axis GNP surface: Cd_mRNA over (mitogen SHH × **HU** dose) — HU (replication stress) elongates S → more EZH2 → more mark → less CyclinD1, the experimentally-actionable lever. (B) MB/vismo surface: Cd_mRNA over (vismodegib HHi × HU), MB context — deepest repression at high vismo + high HU. (C) HU decomposition (GNP): S 5→12 h raises EZH2/mark, lowers Cd_mRNA. (D) vismo decomposition (MB): HHi↑ → Gli-drive↓ → Cd_mRNA↓ toward the MYCN floor, while EZH2/mark **fall** (arrest) — vismo lowers CyclinD1 by drive-removal, residual mark sustains arrest. Cached; `--fresh`. (~13 min) |
| `fig_v44_mitogen_buffering.pdf` | `sim_ezh2_mitogen_buffering.py` | **EZH2 buffers CyclinD1 against mitogen variation — protecting GNP transit-amplifying divisions.** (A) sensitivity + onset: steady CyclinD1 & division rate vs sustained SHH ± feedback — feedback flattens the dose-response (gain 0.85→0.47) and sets the proliferation threshold. (B) fluctuation frequency-response: CyclinD1 fractional transfer gain |δCd/Cd|/|δSHH/SHH| vs fluctuation period (cell-cycle isolated by Fourier projection) ± feedback — slow EZH2 **buffers fast mitogen noise** (gain<no-fb, Berry–Howard low-pass filtering) with a resonance near its ~days timescale. (C) response to partial withdrawal (SHH 0.7→0.3) ± feedback — feedback sharpens the exit decision (ON arrests, OFF coasts). (D) commitment memory: sustained withdrawal → exit vs a transient dip → the committed cell completes its division (Rb–E2F R-point memory → no lost progeny). Robust chunked driver (per-chunk tol-retry + saveState); cached; `--fresh`. (~20 min) |
| `fig_v44_mitogen_sensitivity.pdf` | `fig_v44_mitogen_sensitivity.py` | **mitogen sensitivity in Hh-activity (Gli1) units**, with the correct knobs: (A) **GNP — SHH** is the mitogen (sweep up; vismo only extends the axis below the SHH=0 basal floor); (B) **MB — vismodegib** titrates the tonically-high Hh DOWN toward the MYCN floor (MB CKI tones on). Each: CyclinD1 vs Gli1 ± the EZH2⊣CyclinD1 feedback — the feedback represses/flattens CyclinD1 (lower mitogen sensitivity) and raises the cycling threshold (GNP +fb Gli1~0.008, MB +fb ~0.015; arrest region shaded). NB the basal floor still sits close to threshold (small sub-threshold range). Caches; `--fresh`. (~12 min) |
| `fig_v44_mitogen_paramspace.pdf` | `fig_v44_mitogen_paramspace.py` | **2‑D mitogen‑sensitivity planes**: proliferation (divisions/168 h) over (A) SHH × EZH2/H3K27me3‑feedback strength `f0_mk` (leaky floor; lower = stronger) and (B) SHH × p16, with the cycle/arrest boundary drawn and the defaults marked. The Hh threshold to proliferate **rises** with both stronger feedback and more CKI. Cached grids; `--fresh` recomputes. (~20–30 min cold) |

---

## Distribution model (population-of-models; single-cell-data-grounded)

Virtual-cell populations where each cell draws a small set of DECISIVE parameters from log-normal
distributions. **Only the distribution SHAPE and its CV (variance/mean) are used — never the absolute
means** (fluorescence a.u. are arbitrary); draws are median-1 multipliers, so the mean floats on the
model's own scale. CyclinD1-setpoint spread is grounded on measured single-cell CyclinD1 (G0/1 CV 0.70);
p27 and EZH2 axes are placeholders pending the cleaned p27 fit and the mCherry-Ezh2/TMP series.

| Output PDF | Generating script | What it shows |
|---|---|---|
| `sim_distribution_rescue.pdf` | `sim_distribution_rescue.py` | **Fractional EZH2i rescue — THREE measured-variation axes.** Cells draw CyclinD1 abundance (`cd_scale` CV **0.70**), birth p27 (`P21_div` CV **0.33**; G0 bimodality emerges from the switch), and **EZH2 abundance** (`kTlEZ` CV **~0.55**) — all **DATA-grounded** (MB55 single-cell IF), scale-free (median-1 multipliers). Run through the MB engine ± vismodegib ± EZH2i. **Fractional rescue emerges**; rescue depends on CyclinD1 potential AND EZH2 (more mark to lose). Emergent: EZH2 abundance noise propagates into CyclinD1 spread (model CyclinD1 CV 0.47→0.63 once EZH2 varies). (A) CyclinD1 dist. vs measured log-normal; (B) rescue phase diagram in **(CyclinD1 × EZH2 abundance)** — maps onto the coming mCherry-Ezh2 × CyclinD1 single-cell scatter; (C) rescued fraction. `--full` = N=400 population; default pilot N=24. EZH2-σ calibration deferred to the joint mCherry data. |
| `sim_cyclind1_variance_buffering.pdf` | `sim_cyclind1_variance_buffering.py` | **EZH2 buffers cell-to-cell CyclinD1 variance (scale-free, CV only).** Heterogeneous CyclinD1 setpoint; steady CyclinD1 measured WITH vs WITHOUT EZH2 repression (EZH2i). (A) distributions ÷mean compress under repression; (B) per-cell — EZH2 pulls high-setpoint cells down more (nonlinear high-end cap → variance shrinks); (C) CV comparison. **Prediction: EZH2i broadens the single-cell CyclinD1 CV, mainly the high tail.** |
| `sim_cyclind1_variance_calibrate.pdf` | `sim_cyclind1_variance_calibrate.py` | **Calibrated variance-buffering prediction.** Precomputes the setpoint→CyclinD1 transfer function (EZH2 on/off) once, then computes CV_on/CV_off for any input spread. Solves for σ* where the model's *with-EZH2* CV reproduces the measured **0.70** (σ*=0.73 ≈ measured G2 sdlog 0.738), and reads off the predicted *without-EZH2* CV (~**0.82**) and buffering factor (~**1.18×**). Scale-free (only the measured CV). |
| `sim_p27_bimodality.pdf` | `sim_p27_bimodality.py` | **Bimodal G0 p27 emerges from the transient-G0 / immediate-re-entry commitment switch.** UNIMODAL birth-p27 input → the Skp2–p27–E2f switch splits 2N (G0/G1) p27 into two modes (high uncommitted / low committed), **~4.7× apart, matching the measured ~4× G0 two-component separation**. Modest IF floor (observable offset, not a dynamics change). Open test: model high-component CV 0.44 vs data G0-high 1.29 — the excess data breadth predicts the **arrested/poised contamination the cleaned G0 fit should remove**. |

| `sim_population_mitogen_ramp.pdf` | `sim_population_mitogen_ramp.py` | **Population mitogen ramp — H3K27me3 repression gates ENTRY & EXIT.** Heterogeneous GNP population (three measured-variation axes) ramped SHH up (entry) and down (exit), WITH vs WITHOUT the mark repression (`f0_mk`=0.233 vs 1.0). The mark raises the median Hh threshold to **enter** (0.32→0.54) and to **exit** (0.30→0.46) — a symmetric mitogen gatekeeper. (A) entry CDF, (B) still-cycling CDF, (C/D) threshold distributions. NB this slow-ramp *threshold gating* is the steady-state face of the same slow integrator that *buffers fast dips* (`fig_v44_mitogen_buffering`); the two are timescale-complementary, not contradictory. `--full`=N=120. |
| `sim_ramp_parameter_exploration.pdf` | `sim_ramp_parameter_exploration.py` | **What shifts the Hh thresholds for ENTRY & EXIT: H3K27me3 strength × cell-cycle duration × other features.** Robust metrics (bisection entry threshold; scale-invariant exit threshold = SHH where cycle-avg CyclinD1 drops below half its cycling value). (A/B) entry & exit thresholds over (H3K27me3 strength `1−f0_mk` × cell-cycle duration via `mu`) — both rise with stronger mark AND longer cycle (they interact: short cycle *dilutes* the mark, defeating even a strong mark). (C/D) sensitivity tornados. **Key asymmetry:** ENTRY is dominated by intrinsic features (H3K27me3 strength ≫ CyclinD1 drive > CKI > EZH2 level), while cell-cycle **duration** and **S-phase length (HU)** are the *biggest* movers of EXIT but nearly irrelevant to ENTRY — because those act through the mark only when the cell is cycling (dilution/S-phase), which it is during withdrawal but not before entry. NB roadrunner `reset()` resets species but NOT parameters — every sweep call must re-establish a full baseline (else values leak). |
| `sim_ramp_timing_exploration.pdf` | `sim_ramp_timing_exploration.py` | **TIMING sweep (dense grid): how the H3K27me3→CyclinD1 kinetics (mark turnover `del_mk`, EZH2 half-life `kDeEZ`) and replicative-dilution frequency (cycle period via `mu`) set the entry/exit HYSTERESIS as a function of mitogen-ramp SPEED.** The mark is a slow integrator: on a FAST ramp it lags the mitogen and a hysteresis opens between the ramp-up (entry) and ramp-down (exit) Hh thresholds; on a slow/quasi-static ramp the lag vanishes. 20×16 heatmaps of the gap over (ramp duration × timing value). **(A) del_mk & (B) kDeEZ are clean**: hysteresis biggest at FAST ramp + SLOW kinetics (top-left), i.e. the mark acts as a low-pass filter whose cutoff is set by turnover + EZH2 stability. **(C) mu is confounded** (changing cycle period also changes the cell-cycle *response time*), and **(D) the exit threshold is jaggy** — both because the crossing-threshold metric is per-measurement noisy (does NOT improve with grid density → metric-bound, not undersampled). Superseded readout: the CyclinD1-level-vs-SHH **area** hysteresis (`sim_ramp_hysteresis_area`) is the smooth fix. |
| `sim_ramp_traces_diagnostic.pdf` | `sim_ramp_traces_diagnostic.py` | **Trace diagnostic** — complete trajectories (SHH, CyclinD1, H3K27me3 mark, divisions) for the 4 corners of (H3K27me3 strength × cell-cycle duration), entry & withdrawal ramps. Shows the mechanism directly: short cycle keeps the mark diluted/low (sawtooth halving), long cycle accumulates it; strong mark + long cycle can accumulate enough to collapse CyclinD1 and stall cycling (and is numerically stiff). Confirms the "first-division-during-ramp" metric is unreliable (residual early divisions) → the bisection/threshold metrics are the right ones. |

---

## Calibration / diagnostic scripts (no figure)

| Script | Purpose |
|---|---|
| `validate_v44.py` | Unified validation harness: all experimental‑target checks + figure‑critical behaviors (run this to score the model). |
| `v44_calibrate_hh.py`, `v44_optimize_hh.py` | HH/MYCN→CyclinD1 between‑condition ratio calibration. |
| `v44_calibrate_ezh2.py` | EZH2 within‑cycle gradient + HU boost calibration. |
| `v44_calibrate_final.py`, `v44_probe_sets.py` | constrained whole‑model parameter search (figure‑safe guard). |
| `probe_transient_g0_v2.py`, `analyze_cyclind1_g0.py` | transient‑G0 vs CyclinD1 probes (p27 marker). |
| `probe_two_step.py` | two‑step‑Rb variant probes (`with_two_step_rb=True`). |

---

## v45 figures — ARCHIVED

The v45 stochastic-commitment exploration (model, validator, and its 10-figure suite) has been
moved to **`archive/v45/`** (see `archive/v45/README.md`). **v44 is the committed working model**;
its figures are the ones listed above.
